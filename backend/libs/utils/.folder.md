# backend/libs/utils — Folder Contract
## Architecture Summary
通用工具函数库，服务于 backend/ 下各模块。
仅保留 Ink & Memory backend 共享的通用 helper；Claude Agent SDK 配置直接由 `backend/libs/claude_agent_kit/server/sdk_env.py` 读取 `backend/.env` 中的 `ANTHROPIC_*`。

## File Table
| File | Pos | Function |
|------|-----|----------|
| `env.py` | utility node | `read_int_env` — 从环境变量安全读取整数值 |
| `policy_loader.py` | stub node | `expand_memory_recall_query` — Pawkeyland mem0 查询扩展存根（Ink & Memory 未实现） |
