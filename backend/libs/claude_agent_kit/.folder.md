# backend/libs/claude_agent_kit — Folder Contract

## Architecture Summary
Claude Agent Kit 层，**直接复制自 Pawkeyland `libs/claude_agent_kit/`** 原结构。
保留 `server/`、`messages/` 子目录和所有文件，仅做最小路径适配。
被上层 `backend/claude_agent/` 应用层消费。

## Migration Source
Pawkeyland `libs/claude_agent_kit/` — 直接复制原结构。

**最小适配（不改变业务逻辑）**：
- `_PROJECT_ROOT`：`parents[3]` → `parents[4]`（`backend/` 增加了一层）
- workspace 默认目录名：`claude-agent-workspaces` → `ink-agent-workspaces`
- `server/__init__.py`：MCP server factory import 加 try/except 保护（mcp 包未安装时降级）
- `server/sdk_env.py`：从 `backend/.env` 与当前进程环境读取直接 Claude Code / Anthropic SDK env key，并强制 project-only settings source
- `server/agent_runner.py` / `server/memory_tool.py`：Mem0 配置优先读取 `INK_AGENT_MEM0_*`，旧 `PAWKEYLAND_MEM0_*` 仅作兼容 fallback
- `infrastructure/`、`prompts/policy_loader.py`：stub，necklace/mem0 工具不可用时静默失败

## File Table

| File / Dir | Pos | Function |
|------|-----|----------|
| `__init__.py` | public-api node | 聚合导出（原版）|
| `types.py` | type-contract node | `AgentRunOptions` (including `im_full_access_enabled` and `sandbox_network_mode`), `AgentRunResult`, `AgentStreamingCallbacks`, `ToolEventPayload`, `IClaudeAgentSDKClient` |
| `messages/` | message-builder dir | `build_user_message_content`, `RuntimeContext`, `AttachmentPayload` |
| `server/agent_runner.py` | SDK-wrapper node | `ClaudeAgentRunner` — 含 PreToolUse 工具确认、hook payload snake_case/camelCase 规范化、Settings full-access 显式 allow（`AskUserQuestion` / `mcp__user__ask_user` 例外，仍走前端答案表单）、Settings `sandbox_network_mode="disabled"` 网络工具硬拒绝、auto 模式 workspace `files/` 内置文件工具与低敏工具（查询、`Skill`、`switch_editor`、只读 Bash 子集）显式 allow、高敏执行/写入/交互工具前端确认、`.editor/` 虚拟索引 Read 拦截（`_apply_editor_index_redirect` → 临时文件 → `updatedInput` 重定向）、thinking_delta、BaseExceptionGroup 处理；4 个写工具注册在 `_ALWAYS_CONFIRM_TOOL_NAMES` |
| `server/editor_index.py` | virtual-index-adapter node | `EDITOR_RESOURCES`（统一映射规则）、`is_editor_index_path`、`get_editor_resource_data` — `.editor/` 虚拟路径 → EditorState 字段提取（仅用于读路径 PreToolUse 拦截） |
| `server/notion_snapshot.py` | snapshot-contract node | Notion resource connector canonical snapshot contract: lifecycle states, snapshot metadata, write proposal identity, `.notion/` virtual path resolver, and stale proposal detection; not wired to real Notion API |
| `server/editor_tool.py` | tool-definition node | 4 个写 MCP 工具（write_segment / delete_segment / insert_widget / reply_to_comment）；`_load_session_context` 读 `INK_AGENT_SESSION_ID` / `INK_AGENT_USER_ID`；`_load_editor_state_from_db` / `_save_editor_state_to_db` 直接调用数据库 |
| `server/editor_mcp_server.py` | mcp-server node | `create_editor_mcp_server()` — 将 `EDITOR_WRITE_TOOL_SPECS` 注册为 MCP 写工具 |
| `server/editor_mcp_stdio.py` | stdio-mcp-entrypoint node | stdio 入口，运行 editor MCP 子进程 |
| `server/sessions_tool.py` | session-retrieval mcp node | `mcp__user__get_sessions_range` 工具定义与处理器；按日期范围检索历史 writing sessions，支持 query 字符模糊匹配、labels 过滤、轻量 match 元信息和 vector_query 接口预留 |
| `server/sdk_env.py` | env-config node | SDK 运行时 env 注入；从 `backend/.env` 与当前进程环境合并直接 `ANTHROPIC_*` 等 Claude Code key（Cloud Run Secret Manager 注入值走进程环境） |
| `server/workspace.py` | workspace-manager node | 工作区管理（含 archive extraction、safe path）；`_init_editor_index` 在初始化时创建 `.editor/` 虚拟占位符目录；每次 init 同步 thread-local `.claude/settings.json` `sandbox` 配置，包含只读 runtime dependency allowlist、Docker nested sandbox 自动检测、Settings-backed network policy（open 模式省略 `sandbox.network`），并保持 `.claude/skills/` 可写；skills 同步会导入直接写入 `.claude/skills/` 的真实文件/目录 |
| `server/memory_workspace.py` | memory-workspace node | Procedural Memory 工作空间管理；从分区配置写入 `/memory/` prompt/state 文件，不读取项目 `.claude/memory/` |
| `server/workspace_file_sync.py` | sync-helper node | skills 同步：先将直接写入 `.claude/skills/` 的真实文件/目录导入 `workspace/skills/`，再重建 `.claude/skills/` 发现软链接 |
| `server/simple_cas_client.py` | sdk-client node | `SimpleClaudeAgentSDKClient` — 封装 `ClaudeSDKClient` |
| `server/session_files.py` | session-files node | JSONL 会话文件定位与解析 |
| `server/mcp_server.py` | mcp-factory node | user MCP（touch_animation）— Pawkeyland 专属 |
| `server/memory_*.py` | mem0-mcp nodes | Mem0 记忆 MCP — Pawkeyland 专属，stub 防止崩溃 |
| `server/necklace_*.py` | necklace-mcp nodes | 项圈 IoT MCP — Pawkeyland 专属，stub 防止崩溃 |
| `server/touch_animation_tool.py` | animation-tool node | 触摸动画工具 — Pawkeyland 专属 |

## Key Constraints
- 不依赖 `claude_agent/` 应用层（单向依赖：应用层 → kit 层）
- SDK 配置通过 `backend/.env` 和当前进程环境中的直接 `ANTHROPIC_*` 变量解析；Mem0 仍优先读取 `INK_AGENT_MEM0_*`，旧 `PAWKEYLAND_MEM0_*` 仅作迁移兼容
- `AGENT_CWD` 控制工作区根目录，默认 `ink-agent-workspaces`
- `.editor/` 虚拟索引：Python `_pre_tool_use_hook` 拦截 Read 工具，从内存 `editor_state` 生成临时文件并重定向（CLI ≥2.1 `updatedInput` 格式）
- Pawkeyland 专属 MCP（necklace/mem0/animation）在 Ink & Memory 中不可用，不影响核心 Runner
- Runner SDK 失败路径先用 `logger.exception` 写后台堆栈，再通过 `on_error` 返回增强后的异常上下文
- [Sync] 2026-05-28: 实现 `.editor/` PreToolUse 虚拟索引拦截（Python SDK 层，支持调试器）。
- [Sync] 2026-05-29: 提取 `_apply_editor_index_redirect` 模块级辅助函数（可独立单测）；`_pre_tool_use_hook` 委托调用；补全 `TestEditorIndexRedirectHelper` 测试类（15 个用例，覆盖 redirect / fallthrough / cleanup / 边界路径）。
- [Sync] 2026-05-29: `editor_tool.py` 改为从 `editor_index.EDITOR_RESOURCES` 和 `get_editor_resource_data` 导入，用统一映射规则替代硬编码字段名；`editor_mcp_server.py` / `editor_mcp_stdio.py` 更新 `[Input]` 头部追踪 mapping 来源。
- [Sync] 2026-05-29: MCP 工具重构为写操作专用：删除5个只读工具（list_segments / read_segment / read_session_meta / list_comments / read_comment），添加4个写工具（write_segment / delete_segment / insert_widget / reply_to_comment）；`_load_editor_state` 替换为 `_load_session_context` + `_load_editor_state_from_db`（数据源：数据库，非文件IPC）；`_editor_mcp_stdio_config` 改为传 `INK_AGENT_SESSION_ID` + `INK_AGENT_USER_ID`；写工具注册入 `_ALWAYS_CONFIRM_TOOL_NAMES`；editor MCP 启动条件改为检查 `mcp_env.INK_AGENT_SESSION_ID`。
- [Sync] 2026-06-09: `agent_runner.py` 的 auto 模式按敏感度分流：当前 workspace `files/` 下的内置 `Read` / `Write` / `Edit` / `MultiEdit`、明确只读查询工具、`Skill`、`switch_editor`、只读 Bash 子集返回显式 `permissionDecision:"allow"`；高敏执行/写入/交互工具进入前端确认侧路，批准后返回显式 allow。
- [Sync] 2026-06-09: `agent_runner.py` 兼容 Claude hook payload 的 `toolName` / `toolInput` camelCase 形态，避免 `Skill` 被解析为空工具名后误入前端确认；auto 模式即使有自定义 `allowed_tools` 也会保留必需低敏工具 `Skill`。
- [Sync] 2026-06-09: `AgentRunOptions.im_full_access_enabled` 接入 runner；开启后在 `.editor/` 安全重定向之后，对已暴露工具返回显式 PreToolUse allow。
- [Sync] 2026-06-13: `agent_runner.py` full-access 模式保留 `AskUserQuestion` / `mcp__user__ask_user` 前端确认流，以便收集 answers 并写回 `updatedInput`。
- [Sync] 2026-06-06: `server/memory_workspace.py` 改为 procedural-only Memory Workspace；五个核心 prompt 文件全部从分区配置写入，移除 `.claude/memory/` fallback 和 long-term summary starter；`server/workspace.py` 文档同步为仅由文件接口初始化 `/memory/`。
- [Sync] 2026-06-12: `server/sdk_env.py` 在 `backend/.env` 后叠加当前进程环境中的 SDK 白名单 key，确保 Cloud Run Secret Manager 注入的 `ANTHROPIC_AUTH_TOKEN` 进入 Claude Code 子进程 env。
- [Sync] 2026-06-13: `server/workspace.py` 写入 per-thread Claude Code `sandbox` 设置；Bash workspace confinement 使用 Claude Code 原生 sandbox，不在 runner PreToolUse 中解析 shell。
- [Sync] 2026-06-13: `agent_runner.py` 在 PreToolUse 中对 `Read` / `Grep` / `Glob` / `LS` / `NotebookRead` / `Write` / `Edit` / `MultiEdit` 增加 thread workspace 边界硬拒绝，补齐 Bash sandbox 不覆盖内置文件/搜索工具的问题。
- [Sync] 2026-06-14: `server/workspace.py` sandbox `allowRead` 追加只读运行时依赖目录（temp、Python/Node/toolchain/system libs 等）和 `INK_AGENT_SANDBOX_EXTRA_ALLOW_READ`，但不默认放行项目源码根目录；`agent_runner.py` 测试覆盖 full-access 下 `Grep` 越界仍 hard deny。
- [Sync] 2026-06-14: `server/workspace.py` 检测到 Linux 容器运行时后自动写入 `enableWeakerNestedSandbox=true`，用于 Remote SSH Docker 中的 Claude Code Bash sandbox。
- [Sync] 2026-06-16: `server/workspace.py` sandbox `denyWrite` 不再覆盖整棵 `.claude/`，改为保护 settings/hooks/internal paths，允许 `.claude/skills/` 完整操作。
- [Sync] 2026-06-16: `server/workspace_file_sync.py` 在重建 skills 发现软链接前，会把 `.claude/skills/` 下的真实文件/目录导入 `workspace/skills/`，支持 Agent 直接在 Claude Code canonical skills 目录创建或替换 skill。
- [Sync] 2026-06-16: `server/sessions_tool.py` 的 `get_sessions_range` 增加可配置 `query` 字符模糊检索、`labels` 过滤、`match` 元信息和 `vector_query` 接口边界；不接入向量库。
- [Sync] 2026-06-17: `server/agent_runner.py` 在 SDK 失败路径识别 `apply-seccomp` / bubblewrap 权限错误，并附加 Docker runtime 修复提示到异常 notes。
- [Sync] 2026-06-21: `server/workspace.py` 写入 Settings-backed `sandbox.network`，支持 disabled / allowlist / open 三种网络策略。
- [Sync] 2026-06-21: `types.py` / `server/agent_runner.py` 传递并执行 `sandbox_network_mode="disabled"`，在 PreToolUse 层拒绝 `WebFetch` / `WebSearch` 和常见 Bash 网络命令，且优先于 full-access allow。
- [Sync] 2026-06-25: `server/workspace.py` 在 `sandbox_network_mode="open"` 时省略 `sandbox.network`，避免把 runtime 不支持的 `allowedDomains:["*"]` 写入 thread settings。
- [Sync] 2026-06-28: `server/notion_snapshot.py` adds the minimal design-support contract for connector-owned canonical Notion snapshots and `.notion/` path resolution, intentionally without runner wiring.
- [Sync] 2026-07-20: claude-todo 落地 — `server/workspace.py` 新增 `get_tasks_dir()` / `read_task_items()`（v2 任务目录解析与读时派生）；`server/sdk_env.py` 新增 `apply_task_v2_env_to_options()`（`INK_AGENT_TASK_V2_ENABLED` 门控注入 `CLAUDE_CODE_ENABLE_TASKS=1` / `CLAUDE_CODE_TASK_LIST_ID=main`）；`server/agent_runner.py` 的 `DEFAULT_ALLOWED_TOOLS` 增加四个 v2 任务工具、五 Todo 工具归入 `_LOW_SENSITIVITY_QUERY_TOOL_NAMES`、新增 TaskCreate/TaskUpdate PostToolUse 观察 hook（`INK_AGENT_TODO_EMIT_DEBOUNCE_MS` 防抖）；`types.py` 的 `AgentStreamingCallbacks` 增加 `on_tasks_changed`。
- [Sync] 2026-07-23: SandboxPermissionRequest — `types.py` 的 `AgentRunOptions` 新增 `sandbox_network_allowed_domains`；`server/agent_runner.py` 新增 PreToolUse 步骤 ②.5 `_apply_sandbox_network_permission`（allowlist 命中显式 allow 的低敏子类 `sandbox_network_allowed`；未命中/网络 Bash/open 模式落入确认侧路并携带 `confirmationKind="sandbox_network"` + `networkRequest`；域名匹配对齐 sandbox-runtime：精确大小写不敏感、`*.suffix` 严格子域且不匹配 IP 字面量、裸 `*` 告警且永不命中；待审批网络请求跳过 full-access 与低敏 allow）。
- [Sync] 2026-07-23: SandboxPermissionRequest 运行时代理伴随通道——`server/agent_runner.py` 接入 `ClaudeCodeOptions.can_use_tool=_can_use_tool`：CLI sandbox-runtime 的网络询问（`SandboxNetworkAccess`，`input {"host"}`，不经 PreToolUse）复用同一确认侧路，payload 携带 `confirmationKind="sandbox_network"` + `networkRequest{..., source:"runtime_proxy"}`（PreToolUse 路径 payload 改为 `source:"pre_execution"`）；批准 → `PermissionResultAllow(updated_input)`，拒绝/失败/超时/异常一律 fail-closed → `PermissionResultDeny`（message 指明 host 与 Settings allowedDomains 补救路径）；其他 tool_name 走同一通用链路（含 AskUserQuestion answers 合并）。
- [Sync] 2026-07-26: 拆除 PreToolUse 层网络门禁（错误层级重复）——`server/agent_runner.py` 删除步骤 ②.5 `_apply_sandbox_network_permission` 及 `_match_sandbox_network_allowed_domain` / `_extract_network_tool_host` / `_is_ip_literal` 辅助函数、full-access/低敏跳过守卫、步骤 ⑦ payload 鉴别扩展、`sandbox_network_allowed_domains` 读取与 `ipaddress`/`urlparse` 导入；`types.py` 的 `AgentRunOptions` 移除同名字段。can_use_tool（`SandboxNetworkAccess`）成为唯一网络确认通道，`networkRequest` 不再含 `source`。保留：`SANDBOX_NETWORK_CONFIRMATION_KIND`、`SANDBOX_NETWORK_ACCESS_TOOL_NAME`、`_NETWORK_TOOL_NAMES`、`_is_network_bash_command`、`_apply_disabled_network_permission`、`sandbox_network_mode` plumbing。
- [Sync] 2026-07-26: SDK 迁移 `claude-code-sdk` → `claude-agent-sdk 0.2.128`——`server/agent_runner.py` / `types.py` / `server/simple_cas_client.py` / `server/sdk_env.py` / `server/mcp_server.py` / `__init__.py` 的包名与 `ClaudeAgentOptions` 改名；非机械适配：`debug_stderr` 文件对象废弃 → 新增 `_make_cli_stderr_capture` 经 `options.stderr` 行回调捕获 CLI stderr（`_StderrSentinelArgs` 退役为参考注释）；新 transport 默认优先 bundled CLI（`cli_path` 可覆盖）；`extra_args["setting-sources"]` 透传保留有效（新版仅在设置 `options.setting_sources` 时才自行生成旗标）；hooks / resume / partial messages / `ClaudeSDKClient` API 不变；`CanUseToolShadowedWarning`（allowed_tools 含整工具条目 + can_use_tool）为每进程一次的劝告性警告，属预期。
- [Sync] 2026-07-26: HOTFIX（单一根因，两个生产症状）——`server/agent_runner.py` 将全部 25 处 `HookJSONOutput(...)` 构造调用替换为纯字典字面量：claude-agent-sdk 0.2.128 中 `HookJSONOutput` 为 TypedDict Union（types.py:561）不可调用，构造即抛 `TypeError: 'types.UnionType' object is not callable`；症状 (a) PostToolUse plan-file/tasks 观察器崩溃，(b) PreToolUse allow/deny 被静默丢弃（用户拒绝后工具仍执行）。契约：`{}` 空操作、`{"hookSpecificOutput": {...}}` 决策；导入保留仅作返回类型注解；决策逻辑零改动。
- [Sync] 2026-07-26: `server/workspace.py` 沙箱文件写入策略——(1) 沙箱启用时 `allowWrite` 默认追加 Claude Code 沙箱 TMPDIR（`_sandbox_claude_tmp_write_paths()`：`$CLAUDE_TMPDIR` 或 `/tmp/claude-$UID`），消除 CLI shell hook 写 `cwd-*` 被拒产生的 `zsh: operation not permitted` 噪音；(2) 新增 `_sandbox_fs_extra_write_paths()` 防御性清洗 + `fs_allowed_write_paths` 参数贯穿 `_workspace_sandbox_config` / `sync_workspace_sandbox_settings` / `init_workspace` / `get_or_create_workspace`，用户可写绝对路径追加在工作区与 tmp 路径之后；`denyWrite` 不变（deny 始终优先）。
- [Sync] 2026-07-26: HOTFIX 任务清单定位分歧——0.2.128 内置 CLI 默认启用任务工具（`CLAUDE_CODE_ENABLE_TASKS !== "0"`，字符串实证 `eP(){if(Z.CLAUDE_CODE_ENABLE_TASKS===!1)...}`）且 taskListId 未设置时兜底为 teamName/sessionId（`Ej()` 字符串实证），旧部署未开 `INK_AGENT_TASK_V2_ENABLED` 时任务照跑但写入 per-session 目录，`get_tasks_dir("main")` 定位失败 → 计划与待办弹层永空。`sdk_env.apply_task_v2_env_to_options` 改为无条件注入 `CLAUDE_CODE_TASK_LIST_ID=main`（最低优先级），门控仅追加显式 `CLAUDE_CODE_ENABLE_TASKS=1`；`fn()` 实证新 CLI 仍读取 `CLAUDE_CONFIG_DIR`，`.claude-home/tasks/main` 目标目录不变。
- [Sync] 2026-07-26: CLI 配对修复——`sdk_env.apply_cli_path_to_options()`（`CLAUDE_CODE_CLI_PATH` 存在才生效 → `shutil.which("claude")` → 不设置走 SDK bundled 兜底；显式 cli_path 优先）接入 `agent_runner` options 组装，防止 0.2.128 bundled-first `_find_cli` 遮蔽 Docker apply-seccomp 补丁 CLI；`workspace.py` 新增 `_sandbox_seccomp_config()`：`INK_AGENT_SANDBOX_SECCOMP_APPLY_PATH` 指向存在的 shim 时向 settings.json 写入 `sandbox.seccomp.applyPath`（2.1.220 单一二进制 /proc/self/fd 布局下的 passthrough 新通道）。
- [Sync] 2026-07-26: Route A——`workspace.py` 删除 `_sandbox_seccomp_config()` / `_SANDBOX_SECCOMP_APPLY_PATH_ENV` 与 `_workspace_sandbox_config` 的 seccomp 发射块（settings-seccomp 机制生产证伪后整体拆除）；`sdk_env.apply_cli_path_to_options` 保留（SDK 与打过 vendor 补丁的 npm CLI 2.1.108 配对）。
