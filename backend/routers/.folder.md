# backend/routers — Folder Contract

## Architecture Summary
FastAPI router modules for authenticated REST/SSE API boundaries.
Routers validate request ownership and delegate persistence/workspace behavior to database and service modules.
Business logic stays in focused backend modules rather than route handlers when reusable.

## File Table

| File | Pos | Function |
|---|---|---|
| `__init__.py` | router package init | Marks router modules as an importable package. |
| `admin.py` | admin route node | Register admin and scheduler-related endpoints. |
| `auth.py` | auth route node | Register login/register/import-data endpoints plus `/auth/me` and `/auth/logout` aliases for OAuth-capable clients. |
| `claude_agent.py` | claude-agent route node | Register `/api/claude-agent*` SSE, thread, searchable thread history, session, stop, and tool confirmation endpoints; attachment workspace initialization uses Settings-backed sandbox mode only when Workspace Mode is enabled. |
| `deps.py` | shared dependency node | Provide shared auth/date/text dependencies for routers; current-user auth accepts Bearer tokens or system auth cookies. |
| `device_oauth.py` | device-oauth route node | Register OAuth 2.0 Device Authorization Grant endpoints: `/oauth/device/code`, `/oauth/device/verify`, and `/oauth/token`. |
| `friends.py` | friend route node | Register friend invite and relationship endpoints. |
| `pictures.py` | picture route node | Register daily picture generation/read endpoints. |
| `preferences.py` | preferences route node | Register user preference endpoints. |
| `reports.py` | reports route node | Register analysis report endpoints. |
| `sessions.py` | sessions route node | Register writing session CRUD endpoints plus authenticated `/api/sessions/events` SSE for Edit Session update/delete notifications. |
| `storage.py` | storage route node | Register file storage upload/download proxy endpoints. |
| `system_config.py` | system-config route node | Register user system/SDK config endpoints, including IM full-access approval mode and sandbox network policy; `PUT` returns merged config data for Settings hydration after save. |
| `voices.py` | deck-and-voice route node | Register deck and voice management endpoints, including Memory config fields. |
| `workspace.py` | workspace route node | Register workspace file APIs (`/api/workspace/files*`) including RFC 8187-safe download headers and Settings-backed workspace sandbox initialization. Memory init endpoint and `.claude/memory/` fallback both removed. |
| `reflections.py` | reflections route node | Register Reflections endpoints: `POST /api/reflections/memory-init` (section memory workspace init, user config priority), `GET/PUT/DELETE /api/reflections/config/{section}` (per-user prompt file customisation). |
| `notion.py` | notion route node | Register `/api/connectors*` routes for Notion connector CRUD, auth login/poll, resource discovery, resource selection, snapshot sync, and resource removal. |
| `oauth.py` | google-oauth route node | Register Google OAuth/OIDC login and callback routes backed by Authlib and local system token issuance. |

## Sync

- 2026-06-06: created folder contract; `claude_agent.py` no longer accepts client memory config into service requests.
- 2026-06-06: add `reflections.py` — Reflections section memory workspace init + config CRUD endpoints; memory-init now prefers user DB config over static default.
- 2026-06-06: remove `POST /api/workspace/memory-init` from `workspace.py` (Voice scenario memory workspace initialization deleted).
- 2026-06-09: `system_config.py` accepts `im_full_access_enabled`, the Settings-backed Claude-agent full-access approval flag.
- 2026-06-13: `workspace.py` download responses use a Latin-1-safe `filename` fallback plus UTF-8 `filename*` for non-Latin filenames.
- 2026-06-13: `claude_agent.py` initializes attachment workspaces with the same `workspace_enabled` value used for per-thread Claude Code Bash sandbox settings.
- 2026-06-14: `sessions.py` publishes Edit Session update/delete events after DB writes and exposes `/api/sessions/events` for frontend Agent-write sync.
- 2026-06-21: `system_config.py` accepts `sandbox_network_mode` and `sandbox_network_allowed_domains` for Settings-backed Claude Agent Bash sandbox network policy.
- 2026-06-21: `workspace.py` file APIs initialize workspaces with Settings-backed filesystem and network sandbox policy so file-sidebar refreshes do not revert user-selected network mode.
- 2026-06-22: `claude_agent.py` skips attachment workspace initialization and workspace file sync when `system_config.workspace_enabled=false`.
- 2026-06-23: Added `oauth.py` for Google OAuth and `device_oauth.py` for Device Flow; `auth.py` now exposes `/auth/me` and `/auth/logout`, and `deps.py` accepts auth cookies in addition to Bearer tokens.
- 2026-06-25: `system_config.py` returns `{success, data}` from `PUT /api/system-config`, allowing frontend Settings to hydrate sanitized sandbox network mode/domains after save.
- 2026-06-25: `claude_agent.py` adds `POST /api/claude-agent/threads/{thread_id}/stop` with thread ownership validation for frontend-initiated Agent turn cancellation.
- 2026-06-27: `claude_agent.py` extends `GET /api/claude-agent/threads` with optional Chat history search params (`query`, `search_scope`, `retrieval_mode`, `vector_query`, `min_score`, `limit`) backed by `claude_agent.thread_retrieval`.
- 2026-07-04: Added `notion.py` and mounted the Notion resource connector backend router in `server.py`.
- 2026-07-08: `notion.py` connector list/detail responses now inherit hydrated `sources` from the Notion facade/store so Settings refreshes and Chat linked-resource summaries share persisted selections.
- 2026-07-09: `claude_agent.py` applies `limit`/`offset` to default newest-first thread lists so Chat history panes can scroll-load pages.
- 2026-07-20: `claude_agent.py` adds `GET /api/claude-agent/threads/{thread_id}/plan`（claude-plan，补登记）与 `GET /api/claude-agent/threads/{thread_id}/todos`（claude-todo §5.5），两者均经 `database.get_chat_thread` 归属校验（404 语义一致）。
- 2026-07-21: `storage.py` `_require_storage_auth` now also accepts the `access_token`/`token` login cookie and a `?token=` query param (same precedence as `get_current_user` plus query fallback), fixing 401 on browser-embedded `/api/storage/file/{key}` requests (`<img src>`, download links) that cannot send Authorization headers.
- 2026-07-26: `system_config.py` 接受 `sandbox_fs_allowed_write_paths`（`_sanitize_sandbox_fs_allowed_write_paths`：仅绝对路径、去尾随斜杠、保序去重、32 条 / 512 字符上限、非字符串/非绝对项静默丢弃）；`workspace.py` 的 `_workspace_init_kwargs_for_user` 同步透传该键，避免文件 API 刷新 settings.json 时丢失用户可写路径。
