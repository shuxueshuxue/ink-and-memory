# backend/tests — Folder Contract

## Architecture Summary
Unit and integration test suite for the Ink & Memory backend.
Claude Agent tests use SDK stubs; no real subprocess or server required.
Runner error coverage asserts both `on_error` behavior and backend traceback logging.
Runner PreToolUse policy tests capture the SDK hook closure from constructed
`ClaudeAgentOptions` to verify workspace-file allow, low-risk query/Skill allow,
camelCase hook payload compatibility, Settings disabled-network denial,
Settings full-access allow, the
AskUserQuestion full-access exception, full-access built-in file/search
workspace-boundary denial, and high-risk auto-mode confirmation decisions
without launching Claude Code.
Service SSE mapping tests cover event-shape regressions before frames reach the
frontend transport.

## File Table

| File | Pos | Function |
|------|-----|----------|
| `__init__.py` | package init | marks tests/ as a Python package |
| `test_database.py` | db-layer test | CRUD operations, cascade delete, permission guards |
| `test_api_endpoints.py` | api-integration test | HTTP endpoints, auth, deck/voice lifecycle |
| `test_claude_agent_workspace.py` | workspace test | root resolution, skeleton creation, path traversal rejection, per-thread Claude Code sandbox filesystem/network settings, and bidirectional skills sync for direct `.claude/skills/` writes |
| `test_workspace_router.py` | workspace-router test | workspace file download headers, including Unicode filename RFC 8187 handling, and Settings-backed sandbox network preservation during file API refreshes |
| `test_system_config_router.py` | system-config-router test | system-config PUT response contract and sandbox network domain sanitization for frontend Settings hydration |
| `test_memory_workspace.py` | memory-workspace test | procedural Memory prompt source rules, no `.claude/memory/` fallback, no implicit thread init, explicit file-interface init |
| `test_sessions_tool.py` | session-retrieval mcp test | get_sessions_range range compatibility, query fuzzy retrieval, labels filtering, auto vector fallback, and vector-unavailable boundary |
| `test_notion_snapshot_contract.py` | notion-snapshot contract test | Canonical Notion snapshot virtual path resolution, snapshot resource extraction, snapshot-scoped miss semantics, and write proposal staleness checks |
| `test_notion_operations.py` | notion-operations test | Notion discovery normalization and filtering of system-only People data sources from user-selectable database results |
| `test_notion_auth.py` | notion-auth helper test | Notion CLI home/env resolution, login parsing, poll classification, and auth-status checks |
| `test_notion_store.py` | notion-store test | Connector CRUD, auth-state persistence, selected resource persistence, snapshot identity storage, and thread attachment helpers |
| `test_notion_connector_router_flow.py` | notion-router flow test | FastAPI connector business flow covering create, auth/login, auth/poll, database/page discovery, resource selection, and sync persistence |
| `test_chat_thread_retrieval.py` | chat-history-retrieval test | Chat history retriever plugin behavior, title/message fuzzy matching, scope filtering, auto vector fallback, and vector-unavailable boundary |
| `test_claude_agent_context_builder.py` | context-builder test | system_prompt assembly, Settings SYSTEM_PROMPT lower-priority block, session rendering, graceful DB-error fallback, literal JSON template escaping |
| `test_claude_agent_service.py` | service test | context assembly system_config mapping including Settings SYSTEM_PROMPT handoff, Workspace Mode disabled cwd/workspace skip, sandbox network policy, workspace-local Notion snapshot attach, and service-level SSE event mapping (`tool_input_delta` forwarding for built-in Write previews, editor write session events, cancellation finish sentinel) |
| `test_claude_agent_runner.py` | runner test | streaming callbacks, session_id extraction, error handling, backend traceback logging, tool confirmation, workspace `files/`, Settings full-access including AskUserQuestion exception, Settings disabled-network denial, and sensitivity-based PreToolUse permission policy |
| `test_claude_agent_thread_factory.py` | factory test | flyweight cache, TTL eviction, Phase 1-4 contracts, observer hooks, current-turn stop cancellation |
| `test_server_claude_agent.py` | server-smoke test | route registration, Agent env cleanup, auth enforcement, request model contracts, stop route wiring, and Notion connector router exposure |
| `test_claude_agent_plan.py` | plan-feature test | claude-plan backend contracts: plans-dir resolution containment/None cases, CLAUDE_CONFIG_DIR injection priority, plan-mode/plan-updated SSE frames (not collected), REST plan endpoint (exists:false / 404 / truncation) |
| `test_claude_agent_todo.py` | todo-feature test | claude-todo backend contracts: tasks-dir resolution containment/None cases, v2 env injection gating, read_task_items derivation (internal filter / resolved blockers), v1 TodoWrite SSE capture (not collected), REST todos endpoint (exists:false / 404 / filesystem rebuild), five-tool low-sensitivity classification, INK_AGENT_TODO_MAX_ITEMS truncation |
| `test_session_events.py` | edit-session event-bus test | Edit Session event user isolation and browser SSE payload shape |
| `test_seo_content.py` | seo-content unit test | Verify robots/sitemap/llms generators use configured public frontend/backend URLs and exclude private API paths |
| `picture_dry_run.py` | dev utility | manual dry-run helper; not part of CI |
| `ci-smoke.sh` | CI shell | smoke check script for CI pipelines |
| `README.md` | docs | test overview, run instructions, coverage summary |

## Sync

- 2026-06-13: `test_claude_agent_workspace.py` covers thread-local Claude Code sandbox settings generation; runner tests remain focused on PreToolUse permission policy, not Bash sandbox enforcement.
- 2026-06-13: `test_claude_agent_runner.py` message factories read SDK stub classes from the imported runner module to avoid order-dependent failures after shared stub imports.
- 2026-06-13: `test_claude_agent_runner.py` covers full-access mode preserving AskUserQuestion-style frontend answer forms and `updatedInput.answers`.
- 2026-06-13: `test_claude_agent_service.py` covers `assemble_context` system_config loading before resume DB lookup so `_db` cannot be shadowed by local imports.
- 2026-06-14: `test_claude_agent_workspace.py` covers sandbox runtime read allowlist, project-root exclusion, and Docker nested sandbox auto-detection; `test_claude_agent_runner.py` covers full-access `Grep` outside thread workspace hard deny.
- 2026-06-16: `test_claude_agent_workspace.py` covers sandbox `denyWrite` no longer blocking `.claude/skills/`.
- 2026-06-16: `test_claude_agent_workspace.py` covers importing real files/directories created directly under `.claude/skills/` back into `workspace/skills/` before discovery symlinks are rebuilt.
- 2026-06-14: `test_session_events.py` covers Edit Session event-bus user scoping and SSE payload names; `test_claude_agent_service.py` covers editor MCP write result event publication.
- 2026-06-14: `test_seo_content.py` covers public SEO robots/sitemap/llms content generation from `INK_PUBLIC_BASE_URL` and `INK_BACKEND_PUBLIC_BASE_URL`.
- 2026-06-15: `test_seo_content.py` expects frontend public URLs at origin root after removing `/ink-and-memory/`.
- 2026-06-16: `test_sessions_tool.py` covers configurable Agent session retrieval without a real MCP subprocess or vector store.
- 2026-06-17: `test_claude_agent_runner.py` covers `apply-seccomp` sandbox hint detection; `test_claude_agent_service.py` covers SSE `errorText` including exception notes.
- 2026-06-21: `test_claude_agent_workspace.py` covers disabled/allowlist/open sandbox network settings; `test_claude_agent_service.py` covers system_config sandbox network handoff to workspace init.
- 2026-06-21: `test_workspace_router.py` covers `/api/workspace/files` preserving a disabled sandbox network policy instead of rewriting workspaces with default allowlist settings.
- 2026-06-21: `test_claude_agent_runner.py` covers `sandbox_network_mode="disabled"` denying `WebFetch` and common Bash network commands before full-access or low-sensitivity allow.
- 2026-06-22: `test_claude_agent_context_builder.py` covers Settings SYSTEM_PROMPT lower-priority rendering; `test_claude_agent_service.py` covers prompt config-change rebuild and `workspace_enabled=false` skipping workspace initialization; `test_server_claude_agent.py` covers attachment requests not creating workspaces when Workspace Mode is disabled.
- 2026-06-25: `test_system_config_router.py` covers `PUT /api/system-config` returning merged sanitized sandbox network config so frontend Settings does not revert after refresh.
- 2026-06-25: `test_claude_agent_workspace.py` and `test_workspace_router.py` assert open sandbox network mode omits `sandbox.network` instead of writing unsupported `allowedDomains:["*"]`.
- 2026-06-25: Claude Agent stop coverage added across factory/service/server tests: running `bg_task` cancellation, cancellation SSE `finish` sentinel, and thread-scoped stop route wiring.
- 2026-06-27: `test_chat_thread_retrieval.py` covers the Chat history retriever registry, title/message fuzzy ranking, scope filtering, auto vector fallback, and vector interface-only error.
- 2026-06-28: `test_notion_snapshot_contract.py` covers the Notion connector canonical snapshot contract without requiring real Notion credentials or a Claude runner subprocess.
- 2026-07-04: `test_notion_auth.py` and `test_notion_store.py` cover Notion CLI auth helpers plus connector persistence, `test_notion_connector_router_flow.py` covers the create/auth/discovery/select/sync business route flow, and `test_claude_agent_service.py` / `test_server_claude_agent.py` cover workspace-local Notion snapshot attach and router registration.
- 2026-07-05: `test_claude_agent_service.py` now also asserts the workspace-context Notion summary exposes the attached connector ID and sync cursor, keeping the prompt identity aligned with `.notion/snapshot.json`.
- 2026-07-08: `test_notion_store.py` / `test_notion_connector_router_flow.py` cover selected-resource `sources` hydration on connector responses, and `test_notion_operations.py` covers People system data-source filtering.
- 2026-07-20: `test_claude_agent_plan.py`（claude-plan，补登记）与新增 `test_claude_agent_todo.py`（claude-todo §9 关键用例 ①-⑦）落地；两者均以 `AGENT_CWD` 临时目录 + `tests._sdk_stubs` 运行，无需真实 SDK 子进程。
- 2026-07-23: `test_claude_agent_runner.py` covers SandboxPermissionRequest step ②.5 (claude-agent-sandbox-network-permission-tool.md) — allowlist hits (exact case-insensitive / `*.suffix` strict subdomain) auto-allow; allowlist misses, wildcard-vs-IP-literal, bare `*` (warning + never matches), network Bash in allowlist mode, and every open-mode network request reach the confirmation side-channel with `confirmationKind="sandbox_network"` + `networkRequest`; open mode bypasses both low-sensitivity and full-access allows; direct matcher edge-case tests in `TestSandboxNetworkDomainMatcher`. `test_claude_agent_service.py` asserts `sandbox_network_allowed_domains` passes through to `AgentRunOptions`.
- 2026-07-23: `test_claude_agent_runner.py` 新增 `TestCanUseToolPermissionChannel`（8 例）覆盖 can_use_tool 通道：options 接线非空、SandboxNetworkAccess 批准/拒绝/确认链失败/无回调均正确映射 PermissionResultAllow/Deny（含 host 与 allowedDomains 提示）、通用工具不带 discriminator 且合并 AskUserQuestion answers；既有 networkRequest 断言补充 `source:"pre_execution"`。`_sdk_stubs.py` 与本文件内联 stub 增补 PermissionResultAllow/PermissionResultDeny/PermissionResult/ToolPermissionContext。
- 2026-07-26: `test_claude_agent_runner.py` 删除 PreToolUse 网络门禁测试（14 个 hook 用例 + `TestSandboxNetworkDomainMatcher` 7 例）与 `_capture_pre_tool_use_hook` 的 domains 参数；`TestCanUseToolPermissionChannel` 保留并移除 `source` 断言；`test_claude_agent_service.py` 移除 `AgentRunOptions` domains 透传断言（workspace 初始化断言保留）。套件从 100 例回到 79 例。
- 2026-07-26: SDK 迁移跟进——`_sdk_stubs.py` 与各测试内联 stub 的模块名改为 `claude_agent_sdk`、`ClaudeCodeOptions` → `ClaudeAgentOptions`；`backend/.venv` 引导 pip 后安装 `claude-agent-sdk==0.2.128` 并卸载 `claude-code-sdk`；全量套件 424 passed / 1 skipped / 2 pre-existing failures（test_database、test_editor_index_and_tool），test_api_endpoints 需在线服务器（环境性）。
- 2026-07-26: HOTFIX 跟进——`test_claude_agent_runner.py` 新增 `_hook_specific()` 字典感知读取助手并替换 34 处 `getattr(result, "hookSpecificOutput", ...)`；新增 `TestHookDictLiteralContract`（5 例）：PreToolUse disabled-deny / 低敏 allow / 用户拒绝 deny 返回纯字典、3 个 PostToolUse hook 对非匹配工具返回 `{}` 不抛异常、plan-file/tasks 观察器对非监控路径返回 `{}`（旧 stub 类曾掩盖生产 TypeError）。`_sdk_stubs.py` 注明 HookJSONOutput 在新 SDK 为 Union 不可调用、stub 仅为满足导入。套件 84 例 OK；pytest 429 passed。
- 2026-07-26: 沙箱文件写入覆盖——`test_system_config_router.py` 新增 8 例 sanitizer 契约测试 + 1 例 PUT 透传测试；`test_claude_agent_workspace.py` 更新 allowWrite 断言并新增 4 例（默认含 Claude TMPDIR、CLAUDE_TMPDIR 环境覆盖、额外路径追加且 denyWrite 不变、disabled 沙箱不变）；`test_claude_agent_service.py` 断言 fs 路径传入 `get_or_create_workspace`；`test_workspace_router.py` 新增刷新保留用户可写路径用例。
- 2026-07-26: `test_claude_agent_todo.py` 的 env 注入用例更新为 2026-07-26 语义（`CLAUDE_CODE_TASK_LIST_ID=main` 无条件固定、ENABLE_TASKS 仅门控为真时注入；显式值保留用例不变）。
- 2026-07-26: 新增 `test_sdk_env.py`（7 例）覆盖 `apply_cli_path_to_options` 解析顺序：env 覆盖生效、缺失路径告警下探、which() 命中、无系统 claude 保持未设（bundled 兜底）、显式 cli_path 双向保留、which 调用参数断言；`test_claude_agent_workspace.py` 新增 4 例 `sandbox.seccomp.applyPath` 覆盖（默认无键、shim 存在时写入、缺失忽略、disabled 跳过）。
- 2026-07-26: Route A——`test_claude_agent_workspace.py` 删除 4 例 `sandbox.seccomp.applyPath` 用例（机制已拆除）；`test_server_claude_agent.py` 的 `test_cleanup_preserves_sandbox_runtime_keys` 仅保留 `INK_AGENT_SANDBOX_EXTRA_ALLOW_READ` 断言（该修复仍有效）；`test_sdk_env.py`（cli_path 解析）保留。
