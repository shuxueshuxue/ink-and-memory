# frontend/src - Folder Contract

## Architecture Summary
React 19 app source for the Ink & Memory frontend.
Owns the app root, global styles, i18n setup, and first-level feature folders.
Routes view state through `App.tsx` while feature folders own focused UI behavior.
`App.tsx` keeps the chat workspace mounted after its first visit so chat-local state survives app-level view switches.
The chat view is mounted in a fixed flex viewport so landing panels inherit a stable height and cannot push body/window scrolling.
Connector navigation now routes into Settings' resource-link section; Chat keeps only the lightweight connector landing panel and a Settings CTA.

## File Table

| File | Pos | Function |
|---|---|---|
| `App.tsx` | `frontend app-root node` | Render the authenticated app shell, global views, mobile/desktop navigation, current view state, preserved chat workspace mount, the Settings resource-link section, and the Chat connector landing panel. |
| `App.css` | `app-style sheet` | Provide app-level layout and theme styling. |
| `api/` | `frontend-api client dir` | Backend REST/SSE client helpers, including Claude Agent and Memory workspace initialization calls. |
| `constants/` | `frontend-constants dir` | Centralized frontend constants for storage keys and Edit Session sync timing values. |
| `contexts/` | `frontend-contexts dir` | React context providers for auth and Settings-backed workspace UI state. |
| `engine/` | `editor-engine dir` | EditorEngine state/command model and editor widget data contracts. |
| `hooks/` | `frontend-hooks dir` | Session lifecycle, editing, comments, voice input, inspiration, copy/debounce, file upload, and Edit Session event subscription hooks. |
| `i18n.ts` | `i18n-config node` | Configure frontend translation resources and language defaults. |
| `index.css` | `global-style sheet` | Provide global CSS variables/base styles. |
| `lib/` | `frontend-lib utility dir` | Shared API base/runtime config, chat protocol, transport, and file proxy helpers. |
| `styles/tokens.css` | `frontend-theme-token sheet` | Provide semantic app theme tokens for shared UI styling. |
| `main.tsx` | `react-entrypoint node` | Mount the React application into the browser document. |

## Sync

- 2026-05-25: created folder contract after removing the unused ChatView Settings navigation prop from `App.tsx`.
- 2026-05-29: `App.tsx` keeps `ChatView` mounted after the first chat visit and hides it during other app views so thread, sidebar, input, and streaming state persist across view switches.
- 2026-05-29: `styles/tokens.css` preserves the existing Ink & Memory runtime theme colors as semantic tokens.
- 2026-05-29: `App.css` restores `.notebook-lines` ruled-line colors to existing semantic theme tokens instead of fixed rgba values.
- 2026-06-01: `utils/theme.ts` `initTheme()` no longer persists system pref to localStorage; removes `data-theme` when no explicit pref so CSS media query auto-follows system. `onSystemThemeChange()` added for reactive icon updates.
- 2026-06-05: `components/AnalysisView.tsx` (Reflections page) redesigned to Ciridae dark-noir design system. `components/.folder.md` contract created.
- 2026-06-06: `api/voiceApi.ts` removes Voice scenario memory workspace initialization (`initializeMemoryWorkspace` deleted, calls removed from `chatWithVoiceSSE` and `ensureVoiceThread`); `api/.folder.md` contract created.
- 2026-06-06: `api/voiceApi.ts` migrates Reflections analysis to `analyzeReflectionsSection`: create thread → `POST /api/reflections/memory-init` → `POST /api/claude-agent` SSE (tool_choice=auto, reads WORKFLOW.md) → parse JSON. `ReflectionResult` unified type. `components/AnalysisView.tsx`: per-section independent analyze buttons, per-section loading/streaming/error state, `related_session_ids`-driven related notes.
- 2026-06-12: `lib/apiBase.ts` centralizes runtime API/WS base URL resolution so deployed frontend can call backend through cross-origin requests while local dev keeps the `/ink-and-memory` fallback.
- 2026-06-14: `App.tsx` replaces Agent MCP write 2s blind wait with `/api/sessions/events` event-driven reload and configured fallback; `constants/`, `hooks/`, and `engine/` folder contracts added.
- 2026-06-22: `contexts/WorkspaceContext.tsx` now loads Settings Workspace Mode and gates chat/file workspace UI.
- 2026-06-23: `App.tsx` routes `/oauth/device/verify` to `Auth/DeviceVerificationPage.tsx`; `contexts/AuthContext.tsx` and `components/Auth/*` support Python-backed Google OAuth.
- 2026-07-04: `App.tsx` adds the Resource Connector view entry and connects it to the new dashboard connector page/API client.
- 2026-07-04: `styles/tokens.css` re-centers the semantic paper palette on warmer browns/creams so the Resource Connector page and other shared surfaces look less stark.
- 2026-07-04: `api/resourceConnectorApi.ts` now preserves backend connector UUIDs from create responses so the connector dashboard can authenticate, discover, select, and sync against real `/api/connectors/{uuid}` endpoints.
- 2026-07-04: `api/resourceConnectorApi.ts` now distinguishes draft connectors from active verification sessions so pending connectors can still expose `连接 Notion` before auth login starts.
- 2026-07-05: `App.tsx` makes the connector viewport itself scrollable inside the fixed app shell so `ResourceConnectorPage.tsx` resource-selection and source-list sections remain reachable.
- 2026-07-05: `api/resourceConnectorApi.ts` aligns the resource-selection payload with the backend connector route contract (`selected_databases` / `selected_pages`) so selected sources persist across the save/sync round-trip instead of collapsing to empty lists.
- 2026-07-07: `App.tsx` routes the connector entry into `ChatView.tsx` so the embedded connector workbench lives under the chat shell, matching the new Chat-first resource connector layout.
- 2026-07-07: `App.tsx` mounts ChatView as a fixed flex viewport with `minHeight: 0`/`minWidth: 0`, letting ChatView and ResourceConnectorPage keep connector scrolling inside their own panels.
- 2026-07-08: `App.tsx` routes Connector navigation to Settings, adds `ConnectorSettingsSection`, and keeps Chat on the lightweight connector landing panel with a Settings CTA.
- 2026-07-20: `i18n.ts` adds a top-level `chat` namespace (en + zh, 201 keys each) covering the Chat page: quick actions, date groups, history sidebar, search dialog, workspace tabs, tool confirmation, AskUserQuestion, editor write approvals, input dock, plan panel, Mermaid toolbar, connector landing, shell error, skeleton, and upload errors.
- 2026-07-23: `utils/theme.ts` becomes the single theme source of truth — merges the TopNavBar `ink-theme` toggle and the settings `dashboard-theme` control into one store with `ThemeMode` ('light'|'dark'|'system'), `onThemeChange` subscriptions, and legacy `dashboard-theme` key migration; fixes the two-click toggle / stale icon bug caused by both UIs writing `data-theme` independently.
