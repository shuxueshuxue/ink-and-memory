# frontend/src/components/chat - Folder Contract

## Architecture Summary
Chat workspace UI components for input, messages, tool output, modal/toast feedback, and the composed chat view.
`ChatView.tsx` owns thread/file sidebar/search-dialog state, app-owned history/connector entry rendering, the single pill-style `QuickActionStrip` beneath the composer, and the shell-level error boundary / recovery entry; it delegates message streaming to `ChatPanel.tsx`. Workspace Mode gates file sidebar visibility and workspace file sync entry points.
The landing shell is viewport-contained and width-aligned to the Chat composer/tab bar: ChatView owns the outer flex/min-height chain and the WorkspaceTabBar-level active-tab controls, while history and the `资源连接器` (`ResourceConnectorTabPanel`) own their internal content regions. The connector tab avoids outer card panels; connector status blocks use dashed boundaries without card fill/shadow, and only the linked-resource list scrolls while connector metadata stays fixed.
Settings navigation is not owned by this folder; app-level top/mobile navigation owns that route entry. The Chat `WorkspaceTabBar` (聊天历史/资源连接器 pill switch) is the primary connector discovery entry, and connector configuration belongs to Settings' `ConnectorNotionDetailPage`.

## File Table

| File | Pos | Function |
|---|---|---|
| `AIInputDock.helpers.ts` | `chat-input-dock helper node` | Provide chat input attachment conversion, upload hint, and file-dialog guard helpers. |
| `AIInputDock.tsx` | `chat-input-dock component node` | Render the prompt input, attachment handling, send/stop controls, stop-pending state, and full-access permission badge when Settings enables it; syncs files to workspace only when a workspace session ID is provided. |
| `AskUserQuestionUI.tsx` | `ask-user-question component node` | Render tool-driven user question prompts. |
| `AssistMessagePart.tsx` | `assistant-message-part component node` | Render assistant message parts. |
| `ChatMessageList.tsx` | `chat-message-list component node` | Render the scrollable chat message list, including backend-driven tool approval requests marked by tool metadata. |
| `ChatMarkdown.tsx` | `chat-markdown component node` | Shared GFM Markdown rendering chain for chat surfaces (assistant/user/plan); routes ```mermaid code blocks to `MermaidBlock` and unwraps their `<pre>` wrapper to keep the DOM valid. |
| `ChatPanel.tsx` | `chat-panel component node` | Coordinate chat transport, message state, backend stop requests, scroll-to-bottom affordance, input/message layout, same-tab IM full-access config updates, and Workspace Mode gating for input-dock workspace sync. |
| `ChatView.tsx` | `chat-workspace view node` | Render chat workspace layout, app-selected history/connector landing state, thread history sidebar, centered history search dialog, Workspace Mode-gated file sidebar, quick actions, ChatPanel, and the lightweight connector landing panel. |
| `ChatShellError.tsx` | `chat-shell-error component node` | Render the recoverable shell-level fallback with tab recovery and reload actions when ChatView fails to render. |
| `ConnectorLandingPanel.tsx` | `chat connector landing node (ResourceConnectorTabPanel)` | Render the Chat `资源连接器` tab content — soft-surface empty state with 3 resource-type icons, skeleton loading, and non-button connector status blocks with linked resource previews; only explicit management actions open Settings and focus the resource-link connector section. |
| `CollapsibleSection.tsx` | `collapsible-section component node` | Render expandable/collapsible chat detail sections. |
| `EditorWriteApprovalUI.tsx` | `editor-write-approval component node` | Render specialized confirmation UIs for all 4 editor MCP write tools (write_segment, delete_segment, insert_widget, reply_to_comment). |
| `FileMessagePart.tsx` | `file-message-part component node` | Render file attachment message parts. |
| `Icons.tsx` | `chat-icons component node` | Provide shared SVG icon components for chat/dashboard UI, including chat chrome, history search, and scroll affordance icons. |
| `MermaidBlock.tsx` | `mermaid-block component node` | Render a single Mermaid diagram via lazily loaded mermaid (dynamic-import singleton, strict securityLevel, base theme mapped from CSS design tokens, serialized render queue); debounces streaming source changes and falls back to raw code on failure; toolbar provides 预览/源码 mode toggle, source copy, and PNG export. |
| `PlanPanel.tsx` | `plan-todo button+popover component node` | 浮动控制栏「计划与待办」按钮 + 锚定双区弹层：计划区渲染 Markdown 计划与 planMode 徽标（claude-plan），待办区渲染三态 Todo 清单（claude-todo）；可见性为 plan.exists / planMode!=='none' / todos.exists 任一成立，未读红点两区共用。 |
| `Modal.tsx` | `modal component node` | Render modal overlays for chat workflows. |
| `QuickActionStrip.tsx` | `chat-quick-action-strip component node` | Render the single pill-style quick-action row directly beneath the Chat composer. |
| `Toast.tsx` | `toast component node` | Render transient chat feedback messages. |
| `ToolConfirmationDock.tsx` | `tool-confirmation-dock component node` | Render the floating confirmation panel above `AIInputDock` — generic tool approvals show 拒绝/同意, AskUserQuestion prompts show the option form with 取消/提交. |
| `ToolMessagePart.tsx` | `tool-message-part component node` | Render tool invocation/result message parts. |
| `UserMessagePart.tsx` | `user-message-part component node` | Render user-authored chat text bubbles through GFM Markdown, with a bottom-right copy action under the bubble. |
| `editorWriteTools.ts` | `editor-write-tools utility node` | Provide shared editor MCP write tool name constants and `isEditorWriteTool()` detection for chat message rendering. |
| `toolConfirmation.ts` | `tool-confirmation utility node` | Provide `confirmToolCall()` and the shared pending-confirmation classifiers (`resolvePendingToolConfirmation`, `resolveToolName`, `isAskUserQuestionPart`) driving `ToolConfirmationDock` and the collapsed-row 「待确认」 badge. |
| `toolInputSummary.ts` | `tool-input-summary utility node` | Extract per-tool task summaries (description, shell command, target path/pattern/query) from raw tool input so Terminal cards, collapsed rows, and tool card headers show what the tool is doing. |
| `interaction-utils.ts` | `chat-interaction utility node` | Provide chat interaction helper utilities. |

## Sync

- 2026-05-25: created folder contract after removing the Settings navigation callback from `ChatView.tsx`.
- 2026-05-25: removed frontend customer-context props and request plumbing from chat input/view/panel components.
- 2026-05-25: split chat input helper exports out of `AIInputDock.tsx` to keep the component file component-only.
- 2026-05-26: `ChatPanel.tsx` now defers the message surface until content or an error exists, and `ChatView.tsx` treats loaded history messages as started conversations.
- 2026-05-27: propagate `threadId` from `ChatPanel → ChatMessageList → ToolMessagePart`; fix `confirmToolCall` request body to use snake_case `tool_call_id` + required `thread_id` matching backend `ToolConfirmRequestBody`; render `AskUserQuestion` tool parts directly (not collapsed) so the question form is immediately visible when the backend emits `tool-input-available`; add null guard in `AskUserQuestionUI.useMemo` to prevent crash when tool part is in `input-streaming` state (input not yet available).
- 2026-05-27: `AIInputDock` — add internal `toolChoice` state with `自动` / `逐步确认` segmented toggle in the bottom toolbar; selected value is passed via `onSendMessage`.
- 2026-05-29: fix editor_state missing from agent request — add `editorState` prop to `ChatView` and `ChatPanel`; add `editor_state` field to `ChatApiSchemaRequestBody`; `App.tsx` passes current `state` (EditorState) as `editorState` to `ChatView`; `prepareSendMessagesRequest` includes `editor_state` in the POST body.
- 2026-05-29: add `EditorWriteApprovalUI.tsx` — 4 specialized confirmation UIs for editor MCP write tools plus `isEditorWriteTool()`/`EDITOR_WRITE_TOOL_NAMES` exports; integrate into `ToolMessagePart.tsx` (detect `shouldShowEditorWriteUI`, render `EditorWriteApprovalUI`); `ChatMessageList.tsx` detects editor write tools and renders them directly (not collapsed) with `isManualToolInvocation=true`.
- 2026-05-29: add `EditorWriteCompletedCard` to `EditorWriteApprovalUI.tsx` — completed-state result card replacing Terminal output for editor write tools; exports `EditorWriteOutput` type; `ChatMessageList.tsx` intercepts completed editor write parts and renders `EditorWriteCompletedCard`; fires `editor:jump-to-cell` custom event for cross-view navigation; `App.tsx` listens for event and scrolls to target textarea in writing view.
- 2026-05-29: `ChatView.tsx` restores the full-height workspace shell so `VerticalNav` remains flush-left, while keeping compact header/history/quick-action spacing and existing theme colors.
- 2026-05-29: `ChatPanel.tsx` and `ChatMessageList.tsx` remove the 48rem internal width cap so the Chat page fills the available page width.
- 2026-05-29: `ChatView.tsx` adds a theme-adaptive status bar with New Chat/share/more controls; thread history uses width-transition folding with a title search box; `Icons.tsx` adds local status-bar icons without replacing the restored sidebar icon set.
- 2026-05-29: `ChatView.tsx` renders collapsed-rail history as a floating `最近聊天` list while keeping the expanded-rail search panel behavior.
- 2026-05-29: `EditorWriteApprovalUI.tsx` keeps editor write approval/completed cards text-first in the Chat message surface by removing decorative button/header/status icons.
- 2026-05-29: `ChatView.tsx` moves thread list into `VerticalNav` expanded sidebar (ChatGPT layout); removes separate thread sidebar, flyout dialog, and header bar; adds floating share+more buttons at top-left of main content area.
- 2026-06-01: `ChatView.tsx` no longer creates `/api/claude-agent/threads` on first Chat mount. Empty Chat shows quick actions plus a draft `AIInputDock`; the first send or quick action creates the thread and forwards the queued prompt/tool mode into `ChatPanel.tsx`.
- 2026-06-02: add `UserMessagePart.tsx`; `ChatMessageList.tsx` delegates user text bubbles to it so user prompts render with GFM Markdown.
- 2026-06-06: `ChatMessageList.tsx` recognizes `toolMetadata.approvalRequested=true` and renders the generic Approve/Cancel UI even when the chat is in auto tool mode.
- 2026-06-09: `AIInputDock.tsx` hides the manual approval switch and shows `完全访问` when Settings enables IM full-access; `ChatPanel.tsx` subscribes to same-tab config events so changes apply without page refresh.
- 2026-06-09: SSE reconnect — `ChatView.tsx` bumps `reconnectStreamNonce` when `/status` reports running; `ChatPanel.tsx` subscribes `GET /threads/{id}/stream` via `claude-agent-sse-utils.ts` (replaces background-running banner).
- 2026-06-09: fix reconnect stream stall — stable `onReconnectComplete` + reconnect effect deps limited to `threadId`/`reconnectStreamNonce` so `editorState` parent re-renders do not abort fetch.
- 2026-06-09: `ChatPanel.tsx` shows a floating scroll-to-bottom arrow above `AIInputDock` when the message list is scrolled away from bottom; `Icons.tsx` exports `IconArrowDown` for the affordance.
- 2026-06-12: chat API, transport, file, and tool-confirmation calls use centralized runtime API base so deployment can use direct cross-origin backend requests.
- 2026-06-13: `ChatMessageList.tsx` renders built-in `Write` tool input streaming, final input, output, and errors in a terminal-style file write preview instead of only showing the tool result string.
- 2026-06-14: `ChatMessageList.tsx` collapses long built-in `Write` file previews by default, keeps copy actions full-content, and adds per-message expand/collapse controls.
- 2026-06-14: editor write approval flow forwards `toolCallId` from `ToolMessagePart` through `ChatMessageList`/`ChatPanel`/`ChatView` to `App.tsx` for event-driven Writing view reload de-duplication.
- 2026-06-22: `ChatView.tsx` hides/closes the workspace file sidebar entry when Workspace Mode is disabled; `ChatPanel.tsx` withholds `workspaceSessionId` from `AIInputDock` so chat sends do not sync files into a workspace while disabled.
- 2026-06-25: `ChatPanel.tsx` stop action calls `POST /api/claude-agent/threads/{thread_id}/stop` after aborting local stream consumption; `AIInputDock.tsx` disables the stop button while that backend stop request is pending.
- 2026-06-27: `ChatView.tsx` adds a history search box in the thread sidebar; it calls `/api/claude-agent/threads` with fuzzy retriever params and displays message-match excerpts from backend results.
- 2026-06-28: `ChatView.tsx` moves history search into a centered dialog opened by the history sidebar header search button; `Icons.tsx` adds `IconMessageCircle` for dialog result rows.
- 2026-06-28: `ChatView.tsx` reloads the default thread list whenever the history sidebar opens, with a loading state so first-open history never renders as empty before data arrives.
- 2026-06-28: `ChatView.tsx` removes the search dialog's inline New Chat row; top-level Chat chrome remains the only new-thread entry in this view.
- 2026-07-07: `ChatView.tsx` adds app-selected landing state for `历史对话` and `连接器`, keeps the thread history and embedded `ResourceConnectorPage.tsx` below the composer, and treats connector state as the in-page workbench entry.
- 2026-07-07: `ChatView.tsx` now hoists landing tab state into a shell wrapper, renders a single pill `QuickActionStrip`, and uses `ChatShellError.tsx` as a recoverable shell fallback.
- 2026-07-07: the connector landing state now embeds the dark `ResourceConnectorPage` workbench so the click-through layout shows the compact connector shell, list-style sources, and the shared `分享 / 更多` chrome.
- 2026-07-07: `ChatView.tsx` now tightens the landing shell `min-height: 0` / `overflow: hidden` chain so the connector workbench remains inside the browser viewport and only internal panels scroll.
- 2026-07-07: `ChatView.tsx` removes the duplicate `历史对话` / `连接器` pill row under the quick actions; the app-level route/nav state now owns that switch while the connector workbench starts immediately below the composer.
- 2026-07-08: `ChatView.tsx` swaps the embedded connector workbench for `ConnectorLandingPanel.tsx`, and hands full connector management to `dashboard/ConnectorSettingsSection.tsx`.
- 2026-07-08: rebuilt per docs/prd/notion-session/resource-connector.md and connector-interaction.md's corrected `WorkspaceTabBar` alignment — `ChatView.tsx` renders a fully visible `聊天历史` / `资源连接器` pill switcher (`WorkspaceTabBar`) below the composer; `ConnectorLandingPanel.tsx` becomes the lightweight `ResourceConnectorTabPanel` (toolbar, soft-surface empty state with 3 icons, connector status panels); selecting a connector opens Settings' resource-link section instead of a Chat-owned config page.
- 2026-07-08: removed the obsolete Chat-owned `ConnectorConfigPage.tsx`; Settings `dashboard/ConnectorNotionDetailPage.tsx` is now the only Notion connector detail page.
- 2026-07-08: Chat connector landing, shell error, question approval, tool approval, and editor-write approval surfaces now use semantic background/shadow/state/on-action tokens instead of light-only fills or hard-coded white text.
- 2026-07-08: split `isEditorWriteTool()` / `EDITOR_WRITE_TOOL_NAMES` into `editorWriteTools.ts` so `EditorWriteApprovalUI.tsx` keeps component-only exports for Fast Refresh lint.
- 2026-07-08: `ChatView.tsx` constrains landing tab content to the same 52rem centered width as the composer/tab bar and removes redundant outer history panel chrome; `ConnectorLandingPanel.tsx` now renders non-button connector status panels with authorization/sync/resource metadata and linked-resource previews.
- 2026-07-09: `ChatView.tsx` lifts history search and connector filter/sort controls into the WorkspaceTabBar control row; `ConnectorLandingPanel.tsx` removes its local toolbar, compresses connector metadata into chips, and scrolls only the linked-resource list.
- 2026-07-09: `ChatView.tsx` reloads the default thread list when users switch to or land on `聊天历史`, so stale empty states are replaced by the current history response.
- 2026-07-09: `ConnectorLandingPanel.tsx` listens for Settings connector-change events, reloads connector summaries after resource saves, and renders linked resources with scroll pagination inside the list viewport.
- 2026-07-09: `ChatView.tsx` top-level 新建 action now returns to the default landing state without pre-creating an empty backend thread.
- 2026-07-09: `ChatView.tsx` history panes now fetch default conversations with backend `limit`/`offset` pages and load additional rows on scroll instead of loading all threads.
- 2026-07-09: `ConnectorLandingPanel.tsx` reduces card styling; the root connector tab is unframed, `ConnectorStatusPanel` uses a dashed boundary without card fill/shadow, linked-resource rows/status chips/empty states use soft surfaces, and only explicit controls keep weak boundaries.
- 2026-07-19: add `toolInputSummary.ts` shared helper so tool UI shows what a tool is doing instead of only its result — `ChatMessageList.tsx` Terminal card header displays the task summary (`input.description`, falling back to command/target) and prefers `input.command` for the `$` line (previously only parsed from output text); collapsed tool rows append the summary after the tool name; `ToolMessagePart.tsx` generic card header adds summary + shell command lines.
- 2026-07-20: Mermaid rendering per `docs/design/claude-agent/chat-markdown-mermaid.md` — add `ChatMarkdown.tsx` (shared ReactMarkdown+remarkGfm chain with `code`/`pre` overrides; Mermaid blocks unwrapped from `<pre>`, fixing the `[<pre /> in Markdown ...]` invalid-nesting error) and `MermaidBlock.tsx` (lazy mermaid singleton, strict securityLevel, CSS-token base theme, debounced streaming retries, raw-code fallback); `AssistMessagePart.tsx` / `UserMessagePart.tsx` / `PlanPanel.tsx` delegate to `ChatMarkdown`; `frontend/package.json` adds `mermaid`.
- 2026-07-20: `MermaidBlock.tsx` adds diagram toolbar per design doc §2.6 — 预览/源码 segmented toggle (forced source view when render failed), 复制完整 ```` ```mermaid ```` 围栏 Markdown via shared `useCopy` hook, and 导出 PNG (viewBox sizing, 2x canvas rasterization, paper background fill); render status label moved into the toolbar row.
- 2026-07-20: tool confirmations move out of the message list into a floating dock per `docs/design/claude-agent/claude-agent-tool-confirmation-flow.md` §8 — add `toolConfirmation.ts` (shared `confirmToolCall` + `resolvePendingToolConfirmation` classifiers) and `ToolConfirmationDock.tsx` (floating panel above `AIInputDock` with 拒绝/同意 for approvals and 取消/提交 for AskUserQuestion); `ChatPanel.tsx` derives `pendingConfirmation` from messages and mounts the dock; `ChatMessageList.tsx` renders pending parts as collapsed rows with an amber 待确认/待回答 badge; `ToolMessagePart.tsx` drops the inline Approve/Cancel + AskUserQuestion paths and the `isManualToolInvocation` prop; `AskUserQuestionUI.tsx` adds `framed`/`showHeader`/`submitLabel`/`cancelLabel` props. Editor write tools keep their inline `EditorWriteApprovalUI`.
- 2026-07-20: shrink the dock's AskUserQuestion footprint — `AskUserQuestionUI.tsx` adds a `compact` density prop (0.82rem option/label fonts, 0.3–0.6rem gaps, 0.42rem button/field padding, 3-row textarea), and `ToolConfirmationDock.tsx` caps its height at `min(46vh, 24rem)` with internal scrolling so long forms never fill the chat viewport.
- 2026-07-20: layout change per updated design §8 — `ChatPanel.tsx` now REPLACES `AIInputDock` with `ToolConfirmationDock` in normal flow while a confirmation is pending (input dock hidden until the user decides) instead of floating the panel above the composer; the scroll-to-bottom button keeps its absolute position.
- 2026-07-20: claude-todo §5.6 落地 — `PlanPanel.tsx` 升级为「计划与待办」双区弹层（`IconPlanTasks` 替换 `IconChecklist` 三处引用、按钮 title 改为「计划与待办」、可见性扩展含 `todos.exists`、新增待办分区：#id 序号徽标 + 三态徽章 + active_form 优先 + owner + ⛔ blocked_by + 「暂无待办」空态、未读红点两区共用）；`Icons.tsx` 新增 `IconPlanTasks`；`ChatView.tsx` 并行水合 `hydrateThreadTodos`；`ChatPanel.tsx` 重连路径转发 `todo-updated` 帧。
- 2026-07-20: English i18n for the Chat page — all hardcoded Chinese UI strings in `ChatView.tsx`, `ToolConfirmationDock.tsx`, `AskUserQuestionUI.tsx`, `ChatMessageList.tsx`, `ToolMessagePart.tsx`, `EditorWriteApprovalUI.tsx`, `AIInputDock.tsx`, `ChatPanel.tsx`, `PlanPanel.tsx`, `MermaidBlock.tsx`, `ConnectorLandingPanel.tsx`, `ChatShellError.tsx`, and `Skeleton.tsx` now resolve through the `chat` namespace in `i18n.ts` (en + zh) via `useTranslation`; date-group/relative-time helpers take `t()` and format via `getDateLocale(i18n.language)`; module-level option/label maps became `build*(t)` factories or i18n-key maps.
- 2026-07-21: `FileMessagePart.tsx` routes persisted `/api/storage/file/...` URLs through `withStorageAuthToken()` so historically stored attachments re-attach the current auth token and no longer render/download with 401.
- 2026-07-23: SandboxPermissionRequest network-variant confirmation card — `toolConfirmation.ts` adds the `'sandbox-network'` `PendingConfirmationKind` (driven by `toolMetadata.confirmationKind==='sandbox_network'`) plus `resolveSandboxNetworkRequest()`; `ChatPanel.tsx` carries `networkRequest` into `pendingConfirmation`; `ToolConfirmationDock.tsx` renders host + policy mode + binary 拒绝/同意 (no "remember" in this iteration) with the same Esc / ⌘⏎ shortcuts; `i18n.ts` adds the `chat.toolConfirmation.network*` keys (en + zh). Generic card unchanged when the discriminator is absent (backward compatible).
- 2026-07-23: 网络变体确认卡增加来源提示行——`toolConfirmation.ts` 的 `SandboxNetworkRequestInfo` 增加可选 `source`；`ToolConfirmationDock.tsx` 在策略行下显示「运行时沙箱代理拦截」/「执行前网络请求」（`source` 缺失时不渲染，向后兼容）；`i18n.ts` 增加 `networkSourceRuntimeProxy` / `networkSourcePreExecution`（en + zh）。
- 2026-07-26: 网络变体确认卡移除来源提示行——`toolConfirmation.ts` 的 `SandboxNetworkRequestInfo` 移除 `source`；`ToolConfirmationDock.tsx` 删除提示行渲染；`i18n.ts` 删除 `networkSourceRuntimeProxy` / `networkSourcePreExecution`。`'sandbox-network'` kind、网络卡片与 can_use_tool 流程全部保留。
- 2026-07-26: 用户消息气泡右下角新增复制按钮——`Icons.tsx` 新增共享 `IconCopy`（从 `AssistMessagePart.tsx` 本地实现上提）；`UserMessagePart.tsx` 通过共享 `useCopy` hook 在气泡下方右对齐渲染复制/已复制（✓）按钮，样式与助手消息操作按钮一致；`AssistMessagePart.tsx` 改为引用共享图标。
- 2026-07-26: `PlanPanel.tsx` 待办区防御性加固（React 告警调查）：`TodoListItem` 容忍缺失 `content`/`active_form`/`blocked_by` 字段（原 `todo.blocked_by.length` 遇异形载荷会抛 TypeError 并被 ChatShellBoundary 捕获），列表 key 在 `todo.id` 缺失时回退索引。
