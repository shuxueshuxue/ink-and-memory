# frontend/src/lib - Folder Contract

## Architecture Summary
Shared frontend utility and transport modules for runtime API base config, chat request types, Claude Agent streaming, and file proxy URL construction.
Feature components consume this folder for protocol contracts and API-adjacent helpers rather than duplicating request shapes.

## File Table

| File | Pos | Function |
|---|---|---|
| `apiBase.ts` | `frontend API base-url utility node` | Lazily resolve REST/SSE and WebSocket base URLs from runtime config, Vite env, or root same-origin fallback. |
| `chat-schema.ts` | `chat-schema type node` | Define frontend chat transport request body, attachment, model, tool, and metadata types. |
| `claude-agent-sse-utils.ts` | `claude-agent reconnect SSE utility node` | Parse thread SSE reconnect frames and replay backend events into AI SDK 6 UIMessage parts. |
| `claude-agent-transport.ts` | `transport adapter` | Adapt Claude Agent SSE protocol events into AI SDK UI message chunks, including approval-request metadata for backend-driven tool confirmation UI. |
| `system-config-events.ts` | `system-config-events utility node` | Broadcast same-tab Settings changes, including IM full-access approval mode and Workspace Mode, to chat/file components without page refresh. |
| `toFileProxyUrl.ts` | `file-proxy-url utility node` | Convert stored file keys into frontend-accessible proxy URLs. |

## Sync

- 2026-05-25: created folder contract after removing customer-context fields from the frontend chat request schema.
- 2026-06-06: `claude-agent-transport.ts` now maps `tool-approval-request` SSE frames to `toolMetadata.approvalRequested=true` on the corresponding tool input chunk.
- 2026-06-09: add `system-config-events.ts`; Settings emits IM full-access changes and Chat subscribes for immediate same-tab UI updates.
- 2026-06-12: add `apiBase.ts`; `toFileProxyUrl.ts` now prefixes file proxy URLs with the runtime API base for cross-origin deployments.
- 2026-06-12: `apiBase.ts` resolves `API_BASE` lazily so runtime-config load timing cannot lock deployed API calls to same-origin `/ink-and-memory`.
- 2026-06-12: `claude-agent-sse-utils.ts` now emits AI SDK 6 `dynamic-tool` parts during SSE reconnect replay.
- 2026-06-13: `claude-agent-transport.ts` maps `tool-input-delta` SSE frames to AI SDK 6 `tool-input-delta` chunks; `claude-agent-sse-utils.ts` preserves replay deltas as partial input markers for Write previews until final input arrives.
- 2026-06-15: `apiBase.ts` default same-origin fallback changed from `/ink-and-memory` to the frontend origin root for `/api` and `/polycli`.
- 2026-06-22: `system-config-events.ts` adds Workspace Mode events so Settings can hide/close workspace file UI immediately in the same tab.
- 2026-07-20: `claude-agent-transport.ts` forwards `plan-mode-changed` / `plan-updated`（claude-plan，补登记）与 `todo-updated`（claude-todo §5.4）生命周期帧到 threadId 键控 store，均不映射 UIMessageChunk。
- 2026-07-21: `toFileProxyUrl.ts` appends the current auth token as `?token=` and adds `withStorageAuthToken()` for persisted proxy URLs, fixing 401 on browser-embedded `/api/storage/file/` requests that cannot send Authorization headers.
- 2026-07-23: `claude-agent-transport.ts` and `claude-agent-sse-utils.ts` pass `confirmationKind` / `networkRequest` from `tool-approval-request` SSE frames through to `toolMetadata` so the confirmation dock can render the SandboxPermissionRequest network-variant card (claude-agent-sandbox-network-permission-tool.md §5).
- 2026-07-23: `claude-agent-transport.ts` 的 `BackendToolApprovalRequest.networkRequest` 增加可选 `source` 字段（`'runtime_proxy' | 'pre_execution'`），透传逻辑不变（通用字段已自动带过）。
- 2026-07-26: `claude-agent-transport.ts` 的 `networkRequest` 类型移除 `source` 字段（PreToolUse 门禁拆除后只剩 can_use_tool 单一触发源）。
