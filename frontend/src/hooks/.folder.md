# frontend/src/hooks — Folder Contract

## Architecture Summary
React hooks that coordinate editor lifecycle, text editing, comments, voice input, inspiration, and session event subscriptions.
Hooks own side effects and protocol subscriptions while components keep rendering concerns.

## File Table

| File | Pos | Function |
|---|---|---|
| `useComments.ts` | `comments hook node` | Manage comment grouping, navigation, feedback, and comment chat actions. |
| `useCopy.ts` | `copy hook node` | Provide copy-to-clipboard state with reset delay. |
| `useDebounce.ts` | `debounce hook node` | Debounce changing values for UI/API callers. |
| `useEditSessionEvents.ts` | `edit-session event hook node` | Authenticated fetch-based SSE subscription to `/api/sessions/events` for Edit Session update/delete events. |
| `useFileUpload.ts` | `file-upload hook node` | Upload files to backend storage with auth and progress handling. |
| `useInspiration.ts` | `inspiration hook node` | Manage writing inspiration debounce and visibility state. |
| `useSessionLifecycle.ts` | `session-lifecycle hook node` | Initialize, persist, auto-save only after editor content changes, and switch editor sessions. |
| `useTextCells.ts` | `text-cells hook node` | Manage text cell refs, composition, paste, key handling, and local text mirrors. |
| `useThreadPlan.ts` | `claude-plan store hook node` | threadId 键控 plan store：`useThreadPlan` 订阅 + `applyPlanEvent` SSE 写入 + `hydrateThreadPlan` REST 水合（claude-plan §5.6，补登记）。 |
| `useThreadTodos.ts` | `claude-todo store hook node` | threadId 键控 todos store：`useThreadTodos` 订阅 + `applyTodoEvent` SSE 写入 + `hydrateThreadTodos` REST 水合（claude-todo §5.6）。 |
| `useVoiceInput.ts` | `voice-input hook node` | Manage voice input state and editor insertion behavior. |

## Sync

- 2026-06-14: Add `useEditSessionEvents.ts`; `useSessionLifecycle.ts` skips one auto-save after remote Agent-write reload.
- 2026-06-14: `useSessionLifecycle.ts` tracks editor content signatures so unchanged state notifications do not schedule auto-save debounce or call `/api/sessions`.
- 2026-07-20: Add `useThreadPlan.ts`（claude-plan，补登记）与 `useThreadTodos.ts`（claude-todo）— 两个结构一致的 threadId 键控 store（模块级 Map + `useSyncExternalStore`），消费 plan-* / todo-updated 生命周期帧并经 REST 水合。
- 2026-07-20: `useFileUpload.ts` routes upload/storage error messages through the `chat.upload` i18n namespace via the shared `i18n` instance (module-level helpers cannot call hooks).
