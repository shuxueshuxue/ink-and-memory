# docs/design/claude-agent — Folder Contract

## Architecture Summary
Claude Agent 模块设计文档集，迁移自 Pawkeyland `docs/app/design/`。
涵盖 SDK 交互机制、模块设计、会话管理模式、工作区、持久化、上下文拼接和规划前 prompt 优化。
Pawkeyland 专属领域内容（pet/persona/necklace/mem0）以注释方式保留作参考，不影响阅读。

> [Sync] 2026-07-23: 新增 `claude-agent-sandbox-network-sdk-gap.md` — SDK 通道缺口分析：headless 链路触达不到原生 SandboxPermissionRequest 弹窗的三层实证（Ink 弹窗仅 REPL 渲染 / `can_use_tool` 控制请求后端零处理 / `claude_code_sdk 0.0.25` 无该能力）、残留缺口场景（沙箱内 Bash 访问清单外域名 → fail-closed 403 黑盒）、弥合方案 A（升级 SDK + 桥接）/B（fail-closed + 收窄）/C（双层清单对齐），附持久化路径差异、Bridge 模式、Swarm worker mailbox 路由（双重不可达 + 多 Agent 按 thread 隔离启示）三处概念澄清。
> [Sync] 2026-07-23: 新增 `claude-agent-bridge-mode-forwarding-catalog.md` — Bridge 模式远端转发操作全编目：v1/v2 两代协议、can_use_tool 族四路径（通用权限五方竞争 / 沙箱网络同 host 批量解决 / headless SDK 竞争 / spawn 守护）、消息同步过滤与脱敏 system/init、远端控制请求五 subtype、会话生命周期与 outbound-only mirror、文件产物流、可靠性清单、TTY vs headless 能力对照、§7 对 Ink & Memory 的七条映射启示。
> [Sync] 2026-07-26: 新增 `claude-agent-env-allowlist-audit.md` — `INK_AGENT_*` 环境变量白名单清除机制审计：`server.py::_drop_unsupported_agent_env` 进程级 pop 致 `SANDBOX_SECCOMP_APPLY_PATH`/`SANDBOX_EXTRA_ALLOW_READ` 静默失效（生产事故复盘）、四层环境快照排查方法论、A–F 全量键分类表、清理点错位分析与子进程过滤改进建议、排查命令手册。
> [Sync] 2026-07-29: 新增 `claude-agent-sdk-cli-compatibility-report.md` — SDK × npm CLI 兼容性报告定稿：路线 A 配对（SDK 0.2.128 + CLI 2.1.108 vendor 补丁 + cli_path 锁定）生产验证通过；五大兼容问题实录（序列化方言、HookJSONOutput deny 丢失、bundled 抢占、2.1.220 单二进制与 settings seccomp 证伪、env 四层快照）、兼容性矩阵、残留风险与版本管理规约。
> [Sync] 2026-07-29: 新增 `claude-agent-issue-1151-sandbox-seccomp-en.md` / `-zh.md` — 上游 issue [claude-agent-sdk-python#1151](https://github.com/anthropics/claude-agent-sdk-python/issues/1151) 双语存档：`sandbox.seccomp.applyPath` 被内嵌转换器忽略的证据链（转换器零读取 / 硬编码 `seccomp:jCu()` / shim 零调用）、Docker apply-seccomp passthrough 补丁完整方案（Dockerfile 配方 + 运行时参数 + 安全性论证）。

> [Sync] 2026-07-20: `chat-markdown-mermaid.md` 新增 §2.6 图表工具栏 — 预览/源码分段切换、复用 `useCopy` 复制源码、viewBox + 2x canvas 栅格化导出 PNG；`MermaidBlock.tsx` 同步落地。
> [Sync] 2026-07-20: 新增 `chat-markdown-mermaid.md` — Chat 会话 Markdown 中 ```mermaid 围栏代码块的 SVG 渲染设计：mermaid 动态导入单例 + strict securityLevel + base 主题映射 CSS 语义变量、共享 `ChatMarkdown` 渲染链收敛三处 ReactMarkdown 调用点、`pre` 渲染器解包修正 `[<pre /> in Markdown ...]` 非法嵌套报错、300ms 防抖流式重试与源码降级。
> [Sync] 2026-07-20: 新增 `claude-todo.md` — Todo 功能设计：v1 TodoWrite 流内捕获（P0）+ v2 文件任务捕获（`INK_AGENT_TASK_V2_ENABLED` 门控，`CLAUDE_CODE_TASK_LIST_ID=main` 固定防串扰）、统一 TodoItem 模型、`todo-updated` SSE 与 `GET /threads/{id}/todos` REST 契约、PlanButton 升级「计划与待办」双区弹层（新增 `IconPlanTasks`）、五 Todo 工具低敏分级（含 TaskUpdate 非只读偏差记录）、4 张交互时序图、§13 固化 Expert Prompt Architect 规划前优化工作流。
> [Sync] 2026-07-20: claude-todo §7 业务代码全部落地（后端捕获/契约 + 前端 store/弹层 + 五工具低敏 + `backend/tests/test_claude_agent_todo.py` 26 用例）；`claude-agent-api-contracts.md` §4.5.2/§4.5.4/§4.7.9 与 `claude-agent-permission-policy.md` §3/§8 同步登记。
> [Sync] 2026-07-20: 新增 `claude-task-tools-source-analysis.md` — Claude Code 源码（sourcemap 恢复版）任务工具分析：TodoWrite（纯内存）/ TaskOutput（已废弃，后台任务族）/ TaskCreate / TaskUpdate / TaskList / TaskGet（v2 文件任务族）schema 与机制、`.claude/tasks/` 路径自定义杠杆（仅环境变量与 `--team-name`）、proper-lockfile 两级锁 + highwatermark ID 自增、blockedBy/blocks 双文件冗余边、6 张 mermaid 交互时序图。
> [Sync] 2026-07-20: 新增 `claude-plan.md` — Plan Mode 计划文件落盘路径解析（`CLAUDE_CONFIG_DIR` 主方案 / `plansDirectory` 备选）、后端捕获点、`plan-mode-changed` / `plan-updated` SSE 事件与 `GET /threads/{id}/plan` REST 契约、前端 PlanPanel 挂载点、EnterPlanMode/ExitPlanMode 低敏分级结论。
> [Sync] 2026-05-28: 新增 `claude-agent-prompt-optimization.md`，并将 `claude-agent-context-assembly.md` / `claude-agent-service-design.md` 更新为 Ink & Memory 当前 `assemble_context` 合同。
> [Sync] 2026-05-28: 新增跨模块生命周期综合视图 → [`docs/design/lifecycle/claude-agent-lifecycle.md`](../lifecycle/claude-agent-lifecycle.md)，覆盖 Thread Engine 四阶段、状态机、上下文交互设计、TTL Sweeper、Observer 钩子、工具确认侧路与端点速查。
> [Sync] 2026-05-29: edit-point/ 新增 `editor-state-lifecycle.md`（editor_state 快照完整生命周期）；`workspace-context.md` 补充 §9 双层架构与 editor_state 加载路径说明。
> [Sync] 2026-05-29: `claude-agent-context-assembly.md` 新增 §4.7（existing_session / resume 解析设计）、§7（failure handling 扩展）、§8（跨部署安全边界）、§9（resume 可测试性要求）、§10（checklist 新增 3 条 resume 项）。`claude-agent-session-persistence.md` 更新 §2.2/2.3/2.4（chat_thread 新增 claude_session_id / agent_contract_version 列）、§4.2（_persist_turn 第 5 步 write-back）、§7（Phase 接合点更新）、§8（"未持久化"条目标记为已落地）。
> [Sync] 2026-05-31: 新增 `session-labels-and-retrieval.md` — `user_sessions.labels` 属性 + `mcp__user__get_sessions_range` MCP 工具设计（数据库变更、API 变更、近期上下文格式、env var 注入路径）。
> [Sync] 2026-06-01: edit-point/workspace-switch.md 新增 §8 — Agent 提示工程集成（系统提示 Switch-Editor Workflow + workspace_context Step 0）。
> [Sync] 2026-06-07: `claude-agent-tool-confirmation-flow.md` / `claude-agent-runner-design.md` / `claude-agent-api-contracts.md` 补充 auto 模式敏感度分流策略：workspace `files/` 内置文件工具和低敏查询工具显式 allow；执行/写入/交互/状态切换工具进入前端确认侧路，批准后返回显式 allow；frontend transport 通过 `toolMetadata.approvalRequested` 驱动 auto 模式高敏工具确认 UI。
> [Sync] 2026-06-09: 新增 `claude-agent-permission-policy.md` 作为权限策略真相源；新增缺失的 `claude-agent-prompt-optimization.md`，并在 `context_builder.py` 系统提示中加入 Expert Prompt Architect 规划前优化模板。
> [Sync] 2026-06-09: Settings `im_full_access_enabled` 接入权限策略；`claude-agent-runner-design.md` / `claude-agent-tool-confirmation-flow.md` 说明完全访问模式下已暴露工具直接 PreToolUse allow。
> [Sync] 2026-06-13: 完全访问模式新增 AskUserQuestion 例外；
> `AskUserQuestion` / `mcp__user__ask_user` 仍走前端确认窗口收集 answers。
> [Sync] 2026-06-09: 新增 `sse-reconnect-and-event-bus.md` — SSE 断线重连与 EventBus 设计（v2.1）：Port/Adapter、重连流程、§11 环境变量与 `backend/.env.example` EventBus 段对齐。
> [Sync] 2026-06-06: `claude-agent-tool-confirmation-flow.md` / `claude-agent-runner-design.md` 补充 auto 模式下 workspace `files/` 内置文件工具显式 allow 策略，区分 hook fall-through 与 Claude Code 文件权限层。
> [Sync] 2026-06-06: `workspace-filesystem.md` 校准 Memory Workspace 边界：`init_workspace()` / `assemble_context()` 不初始化 `/memory/`，文件接口 `POST /api/workspace/memory-init` 读取分区配置表写入。
> [Sync] 2026-06-12: `claude-sdk-env-design.md` 补充 Cloud Run / Secret Manager 进程环境注入路径：SDK 子进程 env 来源为 `backend/.env` + 当前进程环境 + 显式 `options.env`。
> [Sync] 2026-06-13: 新增 `claude-agent-workspace-sandbox.md` — Settings
> Workspace Mode 写入每个 thread 的 `.claude/settings.json` sandbox 配置；
> Bash 目录隔离交给 Claude Code 原生 sandbox，不在 PreToolUse 中解析复杂 shell。
> [Sync] 2026-06-14: `claude-agent-workspace-sandbox.md` 增补运行时只读依赖
> allowlist；`Grep`/`Read` 等内置文件工具仍由 PreToolUse 阻断 workspace 外路径。
> [Sync] 2026-06-14: `claude-agent-workspace-sandbox.md` 记录 Remote SSH
> Docker nested sandbox 模式：后端检测到 Linux 容器运行时后自动写入
> `enableWeakerNestedSandbox=true`。
> [Sync] 2026-06-16: `claude-agent-workspace-sandbox.md` 和 interaction plan
> 修正 sandbox `denyWrite`：不再 deny 整棵 `.claude/`，保留
> `.claude/skills/` 完整操作能力。
> [Sync] 2026-06-17: `claude-agent-workspace-sandbox.md` 补充 Docker Compose
> backend 下 bubblewrap 需要 `SYS_ADMIN` 与 unconfined seccomp/AppArmor。
> [Sync] 2026-06-17: `claude-agent-workspace-sandbox.md` 补充 Linux `sbin`
> 目录 runtime allowlist，处理 `bwrap` 在 `/newroot/sbin` 挂载 tmpfs 失败。
> [Sync] 2026-06-21: 新增
> `claude-agent-sandbox-network-interaction-plan.md`，定义 Settings
> 沙箱网络策略、问题分层和最小实现边界。
> [Sync] 2026-06-22: `claude-agent-context-assembly.md` / `claude-agent-service-design.md`
> / `claude-agent-workspace-sandbox.md` / `workspace-filesystem.md` 记录 Settings
> SYSTEM_PROMPT 低优先级拼接，以及 Workspace Mode 关闭时停止初始化 thread
> workspace、隐藏前端文件侧边栏入口。
> [Sync] 2026-06-22: `claude-agent-workspace-sandbox.md`,
> `claude-agent-sandbox-network-interaction-plan.md`, and
> `user-env-injection-design.md` record that Sandbox Network and user env-var
> Settings controls are hidden while Workspace Mode is disabled.
> [Sync] 2026-06-25: `claude-agent-docker-sandbox-egress-incident-plan.md`
> 根据已验证的 `HTTP 403` / `blocked-by-allowlist` 收束为 sandbox network
> allowlist policy deny，并保留 proxy env、local listener、bridge、
> parent proxy/TUN 和 runtime startup 的分层排除路径。
> [Sync] 2026-06-25: `claude-agent-workspace-sandbox.md`,
> `claude-agent-sandbox-network-interaction-plan.md`, and
> `claude-agent-docker-sandbox-egress-incident-plan.md` record that open
> sandbox network mode omits `sandbox.network` instead of writing
> `allowedDomains:["*"]`.
> [Sync] 2026-06-25: 新增 `claude-agent-stop-interaction.md`，定义前端主动停止
> Agent 的最小交互/API/取消路径；明确普通 SSE 断线仍只 unsubscribe，
> stop API 才取消后台 turn。
> [Sync] 2026-06-27: 新增 `chat-history-search.md`，定义 Chat 历史对话搜索交互、
> `/api/claude-agent/threads` 检索参数、插件式 fuzzy/vector retriever 边界与业务时序图。
> [Sync] 2026-06-28: `chat-history-search.md` 更新搜索入口：历史面板标题栏搜索按钮打开居中弹窗，弹窗内展示新聊天入口、分组历史和搜索结果摘要。
> [Sync] 2026-06-28: `notion-point/` 新增 `interaction-snapshot-lifecycle.md`，并将资源连接器数据层收敛为 canonical snapshot source of truth；edit-point workspace/context/switch 文档补齐 Notion connector 边界。
> [Sync] 2026-06-17: `claude-agent-workspace-sandbox-interaction-plan.md` 新增
> `apply-seccomp: Permission denied` 根因归类、最小处理策略和本机 Docker
> 验证清单。
> [Sync] 2026-06-16: `workspace-filesystem.md` 与
> `claude-agent-workspace-sandbox.md` 记录 `.claude/skills/` 真实写入会在
> workspace sync 时导入 `workspace/skills/`。
> [Sync] 2026-06-13: 新增 `write-tool-terminal-preview.md`，定义内置 `Write` 工具 `tool-input-delta` SSE 转发与前端终端式写入预览方案；`claude-agent-service-design.md` / `claude-agent-api-contracts.md` 同步补充事件契约。
> [Sync] 2026-06-14: `write-tool-terminal-preview.md` 补充长文件内容默认折叠、展开/收起与完整复制策略。
> [Sync] 2026-06-16: `session-labels-and-retrieval.md` 增强 `get_sessions_range` 设计：默认 query 字符模糊检索，labels 仅作为过滤维度之一，vector_query 只保留接口边界。

## 迁移策略
| 类型 | 处理方式 |
|------|---------|
| SDK 纯技术文档 | 直接复制，顶部加迁移来源注释 |
| 架构/模块设计 | 路径替换 + pet/persona 段落加注 |
| 会话模式（大文件）| 路径替换 + env 变量替换 + 专属字段加注 |
| 宠物领域专属 | 跳过（LLM动画、宠物MBTI、宠物贴纸、skills流程）|

## File Table

| File | Pos | Function |
|------|-----|----------|
| `../lifecycle/claude-agent-lifecycle.md` | lifecycle-design-doc | **综合生命周期视图**：`/api/claude-agent` 上下文交互设计 + Thread Engine 四阶段（Phase 1–4）+ 状态机（IDLE/RUNNING/DESTROYED）+ 上下文来源优先级 + Intrinsic/Extrinsic 分离 + TTL Sweeper + Observer 钩子完整映射 + 工具确认侧路 + 端点与生命周期速查表 |
| `claude-agent-tool-confirmation-flow.md` | sdk-interaction-doc | SDK 工具确认交互时序图（canUseTool 模式 + Python Future 实现 + Thread Session 集成）；**[2026-05-27]** CLI ≥2.1 `hookSpecificOutput` 新格式（`hookEventName`+`permissionDecision`+`updatedInput`）；`_ALWAYS_CONFIRM_TOOL_NAMES`；前端 `isManualToolInvocation` / `toolChoice` / `toolMetadata.approvalRequested` 路由逻辑；auto 模式 workspace `files/` 文件工具和低敏工具显式 allow + 高敏工具确认策略；full-access 下 AskUserQuestion 仍显示答案表单 |
| `claude-agent-permission-policy.md` | permission-policy-doc | Claude-agent 权限策略真相源：`auto` / `manual` / `none` 模式、低敏/高敏工具分类、`Skill` 与 `switch_editor` 策略、`PreToolUse` hook 输出格式、前端确认触发矩阵 |
| `claude-sdk-message-types.md` | sdk-reference-doc | Claude Code SDK 返回的 SSE 消息事件类型层级与前端消费建议 |
| `claude-agent-thread-session-patterns.md` | session-pattern-doc | Thread Session 四阶段设计模式（Flyweight + State）+ TTL 会话池 + Observer + Phase 1-4；**§11** EventBus 与 Phase 3 解耦（IEventBus Port + BusProxyQueue + run_streaming 重连分支）|
| `claude-agent-runner-design.md` | module-design-doc | ClaudeAgentRunner 模块设计：SDK 封装、MCP 子进程（参考用）、回调分发、PreToolUse 权限策略、异常堆栈诊断 |
| `claude-agent-service-design.md` | module-design-doc | ClaudeAgentService 模块设计：Phase 1/3 业务逻辑、SSE 事件（Ink & Memory 对齐版）、Settings SYSTEM_PROMPT / Workspace Mode 接入、`assemble_context` 不发 SSE/不创建 callbacks 的边界、`_TurnContext`、`_sse_events_to_ui_parts()` 转换 |
| `claude-agent-session-persistence.md` | persistence-design-doc | 会话持久化设计（SQLite `chat_thread + chat_message`）；两阶段：streaming 收集原始 SSE 事件 → `_sse_events_to_ui_parts()` 线性转换；DB schema（`parts TEXT NOT NULL`，无 `content` 列）、前端加载、better-chatbot 映射；**[P2 fix 2026-06-09]** `_persist_user_message` 在推理前写入，`_persist_partial_assistant` 在取消/错误时 flush 部分内容 |
| `chat-history-search.md` | interaction-design-doc | Chat 页面历史对话搜索：标题 + 持久化消息文本检索、历史面板搜索入口、居中搜索弹窗交互、`/api/claude-agent/threads` 参数、插件式 `ChatThreadRetriever`、默认 fuzzy 与 vector interface-only 边界、业务时序图 |
| `claude-sdk-env-design.md` | env-design-doc | SDK env 注入方案；Ink & Memory 直接使用 `ANTHROPIC_*`，不维护 Agent runtime 映射；Cloud Run Secret Manager 注入值通过当前进程环境进入 SDK 子进程 |
| `user-env-injection-design.md` | env-design-doc | 按用户存储的 SDK env 注入方案；用户通过 Settings 存储的 `ANTHROPIC_AUTH_TOKEN` 等 key 注入 Claude SDK 子进程；UI 仅在 Workspace Mode 开启时显示；优先级 user env > backend/.env |
| `claude-agent-context-assembly.md` | context-design-doc | `assemble_context` 生命周期设计：上下文来源顺序、Settings SYSTEM_PROMPT 优先级、Workspace Mode gating、过滤规则、失败处理、输出契约、可测试性与实现检查清单 |
| `claude-agent-prompt-optimization.md` | prompt-design-doc | 规划任务前 Expert Prompt Architect 优化流程：提示词模板、输入输出、与 `assemble_context` / `context_builder.py` 系统提示的关系、测试要求 |
| `claude-agent-workspace-sandbox.md` | workspace-sandbox-design-doc | Settings `workspace_enabled` → workspace lifecycle + per-thread `.claude/settings.json` `sandbox` 配置；关闭时不初始化 thread workspace；Claude Code Bash sandbox 约束 `{AGENT_CWD}/{thread_id}`；说明与 PreToolUse 权限策略的分层关系；记录 Docker nested sandbox 接入；保持 `.claude/skills/` 可写 |
| `claude-agent-sandbox-network-interaction-plan.md` | sandbox-network-interaction-doc | Settings `sandbox_network_mode` / `sandbox_network_allowed_domains` → per-thread `sandbox.network` 交互方案；UI 仅在 Workspace Mode 开启时显示；区分 WebFetch 域名权限、Bash/curl 出站网络和缺失工具三类问题；记录不过度设计边界 |
| `claude-agent-docker-sandbox-egress-incident-plan.md` | sandbox-egress-incident-doc | Docker hosted Claude Agent Bash sandbox 出站失败处理判断与交互/排障设计；当前证据收束为 `blocked-by-allowlist` policy deny，区分 active allowlist/domain pattern、Settings 生效边界、proxy env、local listener、bridge、parent proxy/TUN 和 runtime startup |
| `ai-model-session-flow.md` | flow-design-doc | AI Model 会话流程图（Phase 0-3 完整流程，含宠物相关注释）|
| `workspace-filesystem.md` | workspace-design-doc | 工作区文件系统设计（session 目录骨架、Thread Session 集成、skills 双入口同步）|
| `workspace-design-comparison.md` | workspace-design-doc | TypeScript vs Python 工作区设计对比 |
| `claude-agent-api-contracts.md` | api-reference-doc | `/api/claude-agent/*` 入参与 SSE 响应报文结构整理；**[2026-05-27]** `tool_choice` 新增 `manual` 值；**[2026-06-07]** `tool-approval-request` 触发条件扩展（manual 全工具 + auto 下高敏工具），前端映射为 `toolMetadata.approvalRequested`；§4.6.4 auto+AskUserQuestion SSE 顺序；§4.7.4/4.7.5 真实报文示例 |
| `chat-session-message-er-model.md` | data-model-doc | Chat Session + Claude Message ER 模型（Pawkeyland 参考设计，可扩展至 Ink & Memory）|
| `edit-point/` | design-doc-set | Claude Agent × 编辑器集成三层架构：EditorEngine MCP 工具目录（8工具 Schema）、EditorState `.editor/` 虚拟索引适配器、`<workspace_context>` Prompt 块接入、**`editor_state` 快照完整生命周期**（五阶段+业务时序图，见 `editor-state-lifecycle.md`）、UI 人机协作确认流、多 Agent 冲突仲裁、**`switch_editor` 工作空间切换**（PostToolUse 钩子 + Agent 提示工程，见 `workspace-switch.md`）|
| `notion-point/` | design-doc-set | Claude Agent × Notion 资源连接器工作空间映射设计：Notion CLI 设计原则参考、resource-connector-layer-design 四层工程设计（数据层以 canonical snapshot 为权威状态）、resource-connector-flowcharts 全链路业务流程图、interaction-snapshot-lifecycle 多 Agent 初始化一致性与写入 proposal 生命周期 |
| `session-labels-and-retrieval.md` | feature-design-doc | 笔记标签（`user_sessions.labels`）与跨 Session 协作检索：数据库迁移、`POST /api/sessions` API 变更、近期上下文三天窗口格式（sessionId + labels）、`mcp__user__get_sessions_range` 工具设计与注册、默认 query 字符模糊检索、labels 过滤、vector_query 接口边界、`INK_AGENT_USER_ID` env var 注入路径、完整数据流 |
| `sse-reconnect-and-event-bus.md` | architecture-design-doc | **SSE 断线重连与 EventBus 架构（v2）**：Port/Adapter 模式设计；`IEventBus` Port + `InMemoryEventBus`（asyncio 单实例）+ `RedisStreamEventBus`（多 Pod 生产）+ `BusProxyQueue`（零改造适配 execute_session）；`thread_factory` 重连分支（RUNNING → subscribe + replay）；前端 ChatView 重连取代 banner；部署配置矩阵 |
| `write-tool-terminal-preview.md` | interaction-design-doc | 内置 `Write` 工具写文件时的终端式预览设计：后端转发 `tool_input_delta` 为 SSE `tool-input-delta`，前端 transport 映射到 AI SDK 6 原生 chunk，ChatMessageList 渲染路径、内容、完成状态和长内容折叠 |
| `chat-markdown-mermaid.md` | interaction-design-doc | Chat 会话 Markdown 的 Mermaid 渲染设计：mermaid v11 动态导入单例（strict securityLevel、base 主题映射 CSS 语义变量）、共享 `ChatMarkdown` 组件收敛 AssistMessagePart/UserMessagePart/PlanPanel 三处渲染链、`code`/`pre` 渲染器覆盖与 `<pre>` 非法嵌套修正、流式 300ms 防抖与失败源码降级 |
| `claude-agent-stop-interaction.md` | interaction-design-doc | 前端主动停止 Agent 的最小方案：stop 按钮调用 thread-scoped stop API，后端取消当前 `bg_task` 但不删除 thread，取消路径 flush partial assistant 并关闭 EventBus reader；附过度设计审查 |
| `claude-plan.md` | plan-feature-design-doc | Plan Mode 计划内容捕获与展示契约：`CLAUDE_CONFIG_DIR={workspace}/.claude-home` 落盘路径解析（备选 `plansDirectory`）、计划生命周期状态机（内存态 + 文件系统为唯一持久层）、`plan-mode-changed` / `plan-updated` SSE 事件、`GET /threads/{id}/plan` REST 契约、ChatView「新建对话」控制区上方 PlanPanel 挂载点、EnterPlanMode/ExitPlanMode 低敏分级结论与官方偏差记录 |
| `claude-todo.md` | todo-feature-design-doc | Todo 任务清单捕获与展示契约：v1 TodoWrite 流内捕获（P0 默认）+ v2 文件任务捕获（`INK_AGENT_TASK_V2_ENABLED` 门控 + `CLAUDE_CODE_TASK_LIST_ID=main` 固定 taskListId）、统一 `TodoItem` 模型与 `todo-updated` SSE / `GET /threads/{id}/todos` REST 契约、PlanButton「计划与待办」双区弹层与 `IconPlanTasks` 图标、五 Todo 工具 `low_sensitivity_permission` 分级（TaskUpdate 非只读偏差记录）、v1/v2/重连/权限 4 张时序图、§13 Expert Prompt Architect 规划前优化工作流固化 |
| `claude-task-tools-source-analysis.md` | source-analysis-doc | Claude Code 任务工具源码分析（基于 sourcemap 恢复源码，含文件+行号证据）：三套任务抽象辨析（内存 Todo / v2 文件清单 / 后台运行时任务）、`.claude/tasks/` 路径解析与自定义机制（`CLAUDE_CONFIG_DIR` / `CLAUDE_CODE_TASK_LIST_ID` / `CLAUDE_CODE_TEAM_NAME` / `CLAUDE_CODE_TMPDIR` / `CLAUDE_CODE_ENABLE_TASKS`）、proper-lockfile 列表级+任务级锁、`.highwatermark` 数字 ID 自增、blockedBy/blocks 读时派生、TodoWrite / TaskOutput / TaskCreate / TaskUpdate / TaskList+TaskGet / 跨进程 teammate 共 6 张 mermaid 时序图 |
| `claude-agent-sandbox-network-permission-tool.md` | sandbox-network-permission-doc | SandboxPermissionRequest 权限工具设计（2026-07-26 修订）：现行架构为 SDK `can_use_tool` 单一网络确认通道——CLI sandbox-runtime 代理拦截清单外域名时发起 `SandboxNetworkAccess{host}` 系统级询问，复用 `on_tool_confirmation_request` 链路弹网络变体卡片，批准 → `PermissionResultAllow` / 拒绝 → `PermissionResultDeny`（fail-closed）；PreToolUse 步骤 ②.5 门禁已于 2026-07-26 作为错误层级重复拆除（历史设计见附录 A）；`open` 模式语义回退为不限制出网 |
| `claude-agent-sandbox-network-permission-sequence.md` | sandbox-network-permission-sequence-doc | SandboxPermissionRequest 模块交互图（2026-07-26 重绘）：配置层→CLI 沙箱执行层→can_use_tool→SSE→前端弹窗层总览、can_use_tool 判定流程图、allowlist 清单外域名确认时序图、三模式行为对照表；PreToolUse ②.5 流程已移除（初版见附录） |
| `claude-agent-sandbox-network-sdk-gap.md` | sandbox-network-sdk-gap-doc | SDK 通道缺口分析（2026-07-23；**方案 A 已实施 2026-07-26**）：headless 触达不到原生 SandboxPermissionRequest 的三层实证（Ink 弹窗 REPL 专属 / `can_use_tool` 后端零处理 / 序列化方言过旧）、残留缺口（Bash 访问清单外域名 → fail-closed 403）、弥合方案 A/B/C；实施前提为 SDK 迁移 `claude-code-sdk 0.0.25` → `claude-agent-sdk 0.2.128`（§2.3 后续注记录 0.0.25 旧方言 `{"allow":true}` 被新版 CLI `permissionToolOutputSchema` 拒绝的生产 bug 与对齐证据）、permissions.allow 与本系统持久化路径差异、Bridge 模式（claude.ai 远程控制）机制、§6.3 Swarm worker mailbox 审批路由 |
| `claude-agent-bridge-mode-forwarding-catalog.md` | bridge-forwarding-catalog-doc | Bridge 模式远端转发操作编目（2026-07-23 源码分析）：v1/v2 协议栈与 ReplBridgeHandle 统一接口、can_use_tool 权限族（通用五方竞争 claim / 沙箱网络同 host 批量裁决 / headless SDK remote_control 竞争 / spawn 守护）、消息同步（过桥过滤 / 初始冲刷 / 脱敏 system/init / keep_alive）、remote→local 控制请求五 subtype（initialize/interrupt/set_model/set_max_thinking_tokens/set_permission_mode 策略门控）、会话生命周期（注册/poll/ack/重连/teardown）、outbound-only CCR mirror、文件产物流（入站附件下载 / BriefTool 上传）、可靠性与认证模型、TTY vs headless/SDK 能力对照表、§7 七条对 Ink & Memory 映射启示 |
| `claude-agent-env-allowlist-audit.md` | env-allowlist-audit-doc | `INK_AGENT_*` 环境变量白名单清除机制审计（2026-07-26）：`_drop_unsupported_agent_env` 进程级 pop 根因复盘（四层快照全对但 `os.environ` 被启动代码改写）、镜像/容器/进程启动/进程运行时四层环境快照排查法、A–F 全量键分类表（C 类潜伏调参键、D 类 dotenv 旁路证据）、清理点错位分析与改进建议（子进程 env 过滤 + CI 差集审计）、env 问题排查命令手册 |
| `claude-agent-sdk-cli-compatibility-report.md` | sdk-cli-compat-report-doc | SDK × npm CLI 兼容性报告（2026-07-29 定稿，路线 A 配对已生产验证）：最终配对 SDK 0.2.128 + CLI 2.1.108 + vendor 补丁 + cli_path 锁定、五大兼容问题实录（can_use_tool 序列化方言 / HookJSONOutput Union 不可调用致 deny 丢失 / bundled CLI 抢占配对 / 2.1.220 单二进制与 settings seccomp 证伪 / env 四层快照）、兼容性矩阵、残留风险表、版本管理规约（配对原子变更 + 升级检查清单 + 构建断言）、验证记录 |
| `claude-agent-issue-1151-sandbox-seccomp-en.md` | upstream-issue-doc | 上游 issue [claude-agent-sdk-python#1151](https://github.com/anthropics/claude-agent-sdk-python/issues/1151) 英文版：`sandbox.seccomp.applyPath` 被 CLI 2.1.220 内嵌转换器静默忽略（strings 取证 + shim 零调用实证）、Docker 下无可用沙箱逃生通道的影响分析、四条修复建议、完整复现步骤、Docker apply-seccomp passthrough 补丁方案（Dockerfile 配方 + 运行时参数 + 安全性论证） |
| `claude-agent-issue-1151-sandbox-seccomp-zh.md` | upstream-issue-doc | 上游 issue #1151 中文版：与英文版逐节对应，供团队内部与中文渠道分享 |

## 未迁移文件（跳过）
| 文件 | 跳过原因 |
|------|---------|
| `LLM驱动动画事件图设计方案.md` | Pawkeyland 触摸动画专属，Ink & Memory 无动画层 |
| `workspace-skills-flow.md` | 已迁移到 `docs/design/workspace/workspace-skills-flow.md`，由 workspace 文档集维护 |
| `宠物MBTI创建-微服务设计方案.md` | 宠物领域专属 |
| `宠物专属表情贴纸提示词生成方案.md` | 宠物领域专属 |
- 2026-07-23: `claude-agent-sandbox-network-permission-tool.md` 新增 §5A（can_use_tool 运行时沙箱代理伴随通道，已实现）；`claude-agent-tool-confirmation-flow.md` 新增 §6.3（第二触发源）；`claude-agent-api-contracts.md` §4.5.2 `tool-approval-request` 行登记 `networkRequest.source` 可选字段与 `SandboxNetworkAccess` 触发形态。
- 2026-07-26: 架构反转——`claude-agent-sandbox-network-permission-tool.md` 重大修订（PreToolUse 门禁标记 SUPERSEDED 并移入附录 A，can_use_tool 升为主机制，§4 行为矩阵更新）；`claude-agent-sandbox-network-permission-sequence.md` 重绘为 can_use_tool 单一通道流程图；`claude-agent-permission-policy.md` 回退决策链/矩阵（保留 disabled 硬拒与 §6 代码序对齐）；`claude-agent-tool-confirmation-flow.md` §6.1/§6.2 回退、§6.3 改为唯一触发源；`claude-agent-api-contracts.md` 触发条件④修订并取消 `source` 字段；`claude-agent-sandbox-network-interaction-plan.md` 显式记录 `open` 模式语义回退（不限制出网、无逐次询问）。
- 2026-07-26: SDK 迁移文档同步——`claude-agent-sandbox-network-sdk-gap.md` §5 方案 A 标记已实施、§2.3 增补后续注（0.0.25 控制协议旧方言被新版 CLI 拒绝的实际生产 bug）；`claude-agent-sandbox-network-permission-tool.md` 状态头与 §2 记录迁移前提；`claude-sdk-env-design.md` / `claude-agent-runner-design.md` / `user-env-injection-design.md` / `claude-agent-workspace-sandbox.md` / `claude-sdk-message-types.md` / `workspace-design-comparison.md` / `claude-agent-tool-confirmation-flow.md` / `edit-point/*` 等全部 `claude_code_sdk` / `ClaudeCodeOptions` 引用改名；`setting-sources` extra_args 路径记录为有意保留。
- 2026-07-26: HOTFIX 文档同步——`claude-agent-permission-policy.md` §5、`claude-agent-tool-confirmation-flow.md` §5/§5.2/§6.1、`claude-agent-runner-design.md` §5 异常映射与类型映射表、`edit-point/editor-state-lifecycle.md`、`edit-point/workspace-adapter.md` 的 `HookJSONOutput(...)` 示例全部改为纯字典字面量契约；`docs/prd/Settings.md` 同步更新。
- 2026-07-26: `claude-agent-workspace-sandbox.md` 新增 §2.1 文件写入策略（Claude 沙箱 TMPDIR 默认放行根因分析：cwd-* zsh 噪音；`sandbox_fs_allowed_write_paths` 设置键与 plumbing；denyWrite 优先语义），`claude-agent-sandbox-network-interaction-plan.md` 增补兄弟键指引注。
- 2026-07-26: `claude-todo.md` 契约修订（§5.1/§6.2/§10/§11/§12）——0.2.128 内置 CLI 默认启用任务工具，`CLAUDE_CODE_TASK_LIST_ID=main` 改为无条件注入（修复任务写入 per-session 兜底目录导致弹层永空的线上 bug），`INK_AGENT_TASK_V2_ENABLED` 退化为遗留开关；§12 新增决策记录与回退路径。
- 2026-07-26: CLI 配对决策文档化——`claude-sdk-env-design.md` 新增 §5.5A（cli_path 解析顺序、CLAUDE_CODE_CLI_PATH、Docker apply-seccomp 动机与 bundled 兜底、2.1.220 单一二进制下 seccomp settings 覆盖）；`claude-agent-workspace-sandbox.md` 记录 bundled 遮蔽复发经过与 `sandbox.seccomp.applyPath` 通道；`claude-agent-sandbox-network-sdk-gap.md` §5 实施记录与 `claude-agent-sandbox-network-permission-tool.md` §2 增补 cli_path 锁定 + npm 2.1.220 对齐决策。
- 2026-07-26: Route A 文档——`claude-agent-workspace-sandbox.md` seccomp 节重写（settings 路线证伪证据链：0 settings-key 字符串命中 / `seccomp: jCu()` 硬编码 / shim 未被调用；恢复 2.1.108 vendor passthrough）；`claude-sdk-env-design.md` §5.5A 同步更正并补 SDK 最低版本（2.0.0，仅 warning）；`claude-agent-env-allowlist-audit.md` 新增 §9 补记并调整 B 类表述；`claude-agent-workspace-sandbox-interaction-plan.md` §2.1 增补复发与 Route A 决议。
