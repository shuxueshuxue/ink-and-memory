# docs/design/claude-agent/edit-point — Folder Contract

## Architecture Summary
Claude Agent 与编辑器会话集成的模块化设计文档集。  
以三层架构为核心：EditorEngine 作为 MCP 资源服务器，EditorState 通过 `.editor/` 虚拟索引与 PreToolUse Hook 映射到文件系统工作空间，  
UI/Hooks 层作为人类与 Agent 的协作面，涵盖操作确认、可视化与多 Agent 冲突仲裁。  
[Sync] 2026-05-28: workspace-adapter.md 迁移至 .claude/hooks 拦截设计。
[Sync] 2026-05-29: workspace-context.md 补充 Section 9 — editor_state 双层架构说明与加载路径。
[Sync] 2026-05-29: 新增 editor-state-lifecycle.md — editor_state 快照完整生命周期设计（六阶段、业务时序图、软缓存决策）。
[Sync] 2026-05-29: agent_runner.py 提取 _apply_editor_index_redirect 模块级辅助函数；补全单元测试。
[Sync] 2026-05-29: editor_state 迁移至 AgentRunState 软缓存；mcp-tools.md §5 补充写工具后 DB 刷新时序图；editor-state-lifecycle.md §5 替换为软缓存设计说明（取代原"不持久化"章节）。
[Sync] 2026-05-29: human-agent-collab.md 明确 editor write 聊天确认/完成卡片使用纯文字状态，不使用装饰性图标。
[Sync] 2026-06-14: mcp-tools.md 补充 editor write tool_result 后发布 Edit Session `session_updated(source=agent)` 事件，前端通过 `/api/sessions/events` reload Writing 视图。
[Sync] 2026-06-28: workspace-adapter/context/switch/human-agent-collab 补齐 Notion connector 边界：`.notion/` 来源为资源连接器数据层 canonical snapshot，`switch_editor` 仍只切换 `.editor/` 会话，Notion 写入通过 proposal 确认卡设计。

## File Table

| File | Pos | Function |
|------|-----|----------|
| `edit-point-event-system.md` | overview | 三层架构总览：文档对象模型（档案>片段>评论）、EditorEngine / EditorState / UI 层职责、设计原则、模块索引 |
| `mcp-tools.md` | design-doc | EditorEngine MCP 工具目录：写工具完整 Schema（write_segment, delete_segment, insert_widget, reply_to_comment）、权限矩阵、写工具确认流时序图（含写工具后 editor_state DB 刷新路径） |
| `workspace-adapter.md` | design-doc | EditorState → `.editor/` 虚拟索引适配器：占位符目录结构、PreToolUse 拦截机制、资源映射规范（cells / commentors / tasks / session / full_state）、读写路径分离；§11 说明 Notion connector canonical snapshot 边界 |
| `workspace-context.md` | design-doc | 工作空间上下文接入设计：`<workspace_context>` 块结构、与 `assemble_context` 管道的集成点、`build_workspace_context_block` 调用规范、失败处理、时序图；§9 补充 `editor_state` 双层架构说明，§11 补充 Notion connector context 草案 |
| `editor-state-lifecycle.md` | lifecycle-design-doc | `editor_state` 快照完整生命周期：数据结构定义、六阶段（前端采集→HTTP传输→后端软缓存写入→运行时双激活→MCP写工具后DB刷新→临时文件清理）、完整业务时序图（含软缓存刷新路径）、AgentRunState 软缓存设计、None 语义、双路径读取对比 |
| `human-agent-collab.md` | design-doc | UI/Hooks 层人机协作设计：Agent 操作可视化、确认流程（SSE 事件结构、Approve/Reject）、AgentActionOverlay / AgentOperationHistory 组件设计、对象泳道图；§11 说明 Notion proposal 确认卡边界 |
| `conflict-resolution.md` | design-doc | 多 Agent 冲突解决策略：冲突检测（片段隐式锁、版本号）、冲突信号传递（SSE tool-conflict-detected）、人类仲裁 UI 与流程、锁失效清理、ISSUE 生命周期类比 |
| `workspace-switch.md` | design-doc | 工作空间上下文切换设计：`switch_editor` MCP 工具 Schema、PostToolUse 钩子切换机制（no-op MCP + 主进程享元更新）、权限矩阵（无需用户确认）、时序图；§8 Agent 提示工程集成，§9 明确外部资源切换不复用 `switch_editor` |
