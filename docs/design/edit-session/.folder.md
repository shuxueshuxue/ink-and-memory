# docs/design/edit-session — Folder Contract

## Architecture Summary
编辑器会话模块设计文档集。  
描述 EditorEngine 核心状态机、会话生命周期、事件与状态流、对象协作关系，以及扩展点分析。  
作为 Edit Point / 元操作语义扩展设计的基础参考。

## File Table

| File | Pos | Function |
|------|-----|----------|
| `overview.md` | design-doc | 编辑器会话模块完整设计梳理：核心对象、功能清单、生命周期、事件流、对象泳道图、扩展点与限制 |
| `session-event-driven-sync.md` | design-doc | Session 事件驱动同步机制设计稿：消除 MCP 写 2000ms 盲等待、SessionEventBus SSE 端点、toolCallId 去重、自动保存幂等保护、过度设计审查与实现清单 |

## Sync

- 2026-06-14: `session-event-driven-sync.md` 收敛为 v3 最小实现稿；`overview.md` §6.4 改为 Agent 写工具完成事件 + fallback reload，不再使用固定 2 秒等待。
- 2026-06-14: `session-event-driven-sync.md` 补充自动保存 editor content signature 去重策略：无内容变化时不创建 3 秒 debounce timer，也不调用 `/api/sessions`。
