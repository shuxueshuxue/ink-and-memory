# docs/deploy/

## 架构概述
发布体系文档入口，区分本地直跑、Docker 容器、Remote SSH 和 Google Cloud Run 四类发布路径。

> [Sync] 2026-06-14: Remote SSH Docker 后端镜像和 Compose 文档补充
> Claude Code Bash sandbox 依赖、自动 nested sandbox 检测与
> `seccomp=unconfined` 运行约束。
> [Sync] 2026-06-17: Docker Compose 与 Remote SSH Compose 补充 backend
> `SYS_ADMIN` 和 `apparmor=unconfined`，解决 bubblewrap
> `Failed to make / slave`。
> [Sync] 2026-06-17: Docker/Remote SSH 排障补充 `bwrap` 在
> `/newroot/sbin` 挂载 tmpfs 失败；backend 镜像需包含标准 `sbin`
> 目录，workspace sandbox allowlist 需放行已存在的 `sbin` 运行时路径。
>
> [Sync] 2026-06-15: 发布验收 URL 统一为前端根路径 `/`，
> SEO 静态入口为 `/robots.txt`、`/sitemap.xml`、`/llms.txt`。
>
> [Sync] 2026-06-16: Remote SSH 数据维护收敛为
> `backup` / `upload` / `download`，上传后 force-recreate Compose 服务加载数据库。
>
> [Sync] 2026-06-23: 发布文档补充生产 Google OAuth public URL、
> Secure/SameSite cookie、credentials CORS 与 Secret Manager 配置要求。
>
> [Sync] 2026-06-23: Docker/Remote SSH 文档补充 Mihomo TUN
> `tun-proxy`，backend 共享 proxy 网络命名空间，真实 Clash 配置保持
> gitignored。
>
> [Sync] 2026-06-25: Remote SSH sandbox 排障补充 `HTTP 403` /
> `blocked-by-allowlist` 分支，优先按 sandbox network allowlist policy
> deny 处理；无该 header 的 reset/502/timeout 再走子进程代理出口排障。

## 文件清单

| File | Pos | Function |
|------|-----|----------|
| README.md | 发布文档入口 | 四类发布路径分流、平台脚本入口、对比表、生产认证配置、Docker TUN 出站和维护规则 |
| overview.md | Cloud Run 部署主文档 | 现有完整云发布流程：存储初始化、环境变量配置、Google OAuth/CORS/cookie 生产配置、跨域 API、发版步骤、数据持久化说明 |
| data-sync.md | 云数据同步 | 本地与 GCS bucket 之间的数据上传、下载、备份、查看方法，以及同步重启后的 Claude Agent 鉴权排障 |
| remote-ssh.md | Remote SSH 部署 | 说明 SSH/rsync 同步到远程 Docker 服务器，并用 docker-compose 构建、启动、验证和回滚；包含 Mihomo TUN 出站、生产 OAuth/CORS/cookie 配置、Remote SSH 数据备份/上传/下载维护和 Claude-agent Docker sandbox 排障 |
| release-system-design.md | 发布体系设计稿 | 当前问题判断、发布方案、文档/脚本改造计划、验收清单 |
| ../../deploy/google-cloud/sync-data.sh | Google Cloud 同步脚本 | 上传本地 SQLite 数据到 GCS 并自动重启后端服务；`../../deploy/sync-data.sh` 仅保留兼容委托 |
