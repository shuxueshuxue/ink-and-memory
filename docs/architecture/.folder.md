# docs/architecture — Folder Contract

## Architecture Summary
Backend architecture documentation for Ink & Memory.  
Covers module boundaries, service layers, migration maps, API surfaces, and data flows.

## File Table

| File | Pos | Function |
|------|-----|----------|
| `claude-agent.md` | architecture-doc | Claude Agent module architecture: directory layout, API endpoints, layer design, migration mapping, config, data flow, error handling, test strategy, and isolation notes |
| `项目架构设计说明.md` | architecture-overview | Project architecture overview for Vite + FastAPI modules, auth middleware boundaries, adapter types, Mermaid auth sequences, and target OAuth/Device Flow module placement. |
| `auth.md` | auth-architecture-doc | Google OAuth / OIDC architecture plan: Python Authlib backend as auth center, local user binding, token strategy, env vars, data tables, security boundaries, and tests. |
| `auth-device.md` | device-flow-architecture-doc | OAuth 2.0 Device Authorization Grant plan for CLI/Desktop/Agent/MCP clients, including routes, state transitions, polling rules, security, Mermaid flows, and tests. |

## Sync

- 2026-06-16: `claude-agent.md` documents that `workspace_file_sync.py` is migrated and imports direct `.claude/skills/` writes into `workspace/skills/` before rebuilding discovery symlinks.
- 2026-06-23: Added OAuth architecture docs for Python Authlib Google OAuth and Device Flow; `项目架构设计说明.md` now documents auth middleware boundaries, adapter types, and Mermaid authentication sequences.
- 2026-06-25: `claude-agent.md` endpoint table includes the thread-scoped stop API for cancelling the current running Agent turn without deleting chat history.
- 2026-07-26: `claude-agent.md` 的 SDK 引用随 `claude-code-sdk` → `claude-agent-sdk 0.2.128` 迁移更新为 `claude_agent_sdk`。
