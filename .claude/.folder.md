# Folder Plan: .claude

<!-- vibe-folder-map:start -->
## Architecture
- Scope: .claude
- Function: Store project-scoped Claude Code settings, lifecycle hooks, and workspace skills used by local agent sessions.
- Sync: 2026-05-10 restored Mem0 SessionStart/Stop hooks from origin/claude-runner. The hooks use the project `PAWKEYLAND_MEM0_API_KEY` and `PAWKEYLAND_MEM0_API_HOST` data-plane config while the runner injects the app-resolved business user/persona memory key through the claude-runner `MEM0_USER_ID` contract.
- Sync: 2026-06-09 patched the ClawHub `a-stock-monitor` skill locally so `stock_cache_db.py` requires `STOCK_MONITOR_DB_PATH` instead of using the upstream hard-coded SQLite path.

## Files
| File | Pos | Function |
|---|---|---|
| `settings.json` | `config` | Project Claude Code hook registry, including PreToolUse file protection and Mem0 SessionStart/Stop lifecycle hooks |
| `.clawhub/lock.json` | `state` | ClawHub-managed lockfile for project-scoped skills installed under `skills/` |
| `hooks/` | `dir` | Shell/Python hook shims for protected-file checks and cross-session Mem0 context |
| `skills/` | `dir` | Project-scoped Claude Code skills synced from allowed workspace skill sources, including monitored ClawHub finance/quant skills |
| `worktrees/` | `dir` | Local Claude worktree state; not part of application runtime |
<!-- vibe-folder-map:end -->
