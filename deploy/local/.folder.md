# deploy/local/

## 架构概述
本地直跑发布脚本目录，复用 `backend/server.py` 与 `frontend/package.json` 的现有启动方式。

## 文件清单

| File | Pos | Function |
|------|-----|----------|
| deploy.sh | 本地发布入口 | 检查本地依赖和配置，构建前端，启动/停止本地 backend 与 frontend 进程，执行本地验证 |
